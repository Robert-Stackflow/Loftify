# Loftify 待办与验收清单

> 最近整理：2026-08-24
> 参考基线：CloudOTP / awe_chewie
> 当前主线：除明确暂缓的官方接口能力外，功能与交互任务均已完成；现在进入严格排在最后的全软件视觉重构阶段。

## 任务总览

- 功能与体验任务：107 项；已完成 102 项，待完成 5 项。
- 重复验收项：8 项；它们是每轮交付都要执行的流程，不计入功能任务完成率。
- 当前焦点：2026-08-24 实机反馈的视觉回归校准已全部完成；划线评论及其他依赖官方接口的能力继续暂缓。
- 紧接处理：继续阶段 B2C 的卡片、菜单、通用 Panel 与状态矩阵统一；其后按文档顺序进入剩余设计系统迁移。
- 当前设备：MuMu Android 12（1080×1920，ADB `127.0.0.1:16384`）在线，Flutter 调试会话支持 Hot Reload / Hot Restart；独立 Android 真机当前未连接。

### 未完成任务分布

| 优先级 | 待完成 | 范围 |
| --- | ---: | --- |
| P0 | 0 | 本轮视觉回归已清零 |
| P1 | 0 | 本轮交互体验回归已清零 |
| P2 | 4 | 划线评论（依赖官方接口，暂缓） |
| 最终阶段 | 1 | 全软件视觉设计系统与页面重构 |

### 2026-08-24 实机视觉回归校准

- [x] `REDESIGN-R01` 将全局 Material 字体映射恢复到原应用的紧凑基线；设计 Token 只供明确迁移的新组件使用，正文阅读字号单独生效，禁止让旧页面整体放大。
- [x] `REDESIGN-R02` 恢复并精修帖子详情页 AppBar 合集入口，以及正文底部的合集、粮单、标签等原有轻量控件；不得用统一大卡片或大按钮覆盖原有信息密度与业务层级。
- [x] `REDESIGN-R03` 逐项恢复被错误移除的卡片、列表项、Panel 与交互控件背景，浅色模式维持白色页面与柔和浅灰内容层级，不能出现控件边界消失。
- [x] `REDESIGN-R04` 重新设计“我的”主导航页面的信息层级和内容中心；装扮入口移入内容中心，保留已有下载管理及全部业务入口。
- [x] `REDESIGN-R05` 四个主导航不再使用完整横向 AppBar，改为相互一致的悬浮顶部控件：首页左侧用户头像并跳转“我的”；搜索左侧用户头像、右侧搜索按钮；关注使用悬浮 Tab 栏；“我的”左侧通知、右侧仅保留设置和深浅色切换。
- [x] `REDESIGN-R06` 首页悬浮栏支持沉浸式滚动：未滚动时用安全区与顶部 padding 保持原有首屏观感；滚动后内容可自然扩展到状态栏区域，悬浮控件原位可读且不遮挡内容。
- [x] `REDESIGN-R07` 重做底部导航 Lottie：未选中为克制线性轮廓，选中通过自然动画过渡到清晰的面性状态，四个图标均应有足够可感知但不过度的动态反馈。
- [x] `REDESIGN-R08` 优化底部导航随滚动显示 / 隐藏的位移、透明度、缩放和曲线衔接，快速反向滚动不跳变，减少动态模式保持即时稳定。
- [x] `REDESIGN-R09` 修复下拉刷新层级与尺寸回归：动画不能像被列表盖住；指示器尺寸、触发距离和拖拽缩放必须协调，保持上一版从小到大显现及回弹体验。
- [x] `REDESIGN-R10` 普通页面 AppBar 的返回、更多、下载等独立图标按钮统一使用圆形点击面，不再默认使用矩形圆角；明确传入圆角的行内工具按钮保持原布局语义，横屏返回按钮遵循同一规则。

### 当前迭代看板

| 状态 | 任务 | 本轮目标 |
| --- | --- | --- |
| 阶段完成 | `BASE-*`、`UI-*`、`L10N-01` | 基础组件、主题、本地化、加载动画和全局页面视觉已收口 |
| 阶段完成 | `POST-01`～`POST-07` | 帖子卡片、1～9 图、异常内容回退和布局切换已通过自动化与模拟器验收 |
| 本轮完成 | `PROFILE-01`～`PROFILE-03` | 个人主页信息层级、统一卡片、背景衔接和吸顶视觉已通过自动化与模拟器验收 |
| 本轮完成 | `DETAIL-01`～`DETAIL-04` | 评论层级、详情底栏、详情页基础视觉和局部错误回退已完成自动化与模拟器验收 |
| 本轮完成 | `PROFILE-04`、`DETAIL-05`、`SCROLL-01`～`SCROLL-04` | 嵌套滚动、刷新动画、空态 Sliver、重复请求与长文重排已通过自动化与模拟器验收 |
| 本轮完成 | `LIST-02`、`LIST-04`、`LIST-05`、`LIKES-01`～`LIKES-03` | 消除重复分页与滚动监听，完成长列表懒构建、位置恢复、异常卡片和喜欢日期分组同步 |
| 本轮完成 | `LIST-01`、`LIST-03` | 搜索与动态流完成公共分页迁移，图片按显示尺寸等比解码，离屏列表与图片状态按需回收 |
| 本轮完成 | `IMAGE-01`～`IMAGE-05` | 原图链路、相邻页预热、失败重试、灯箱翻页缩放、下拉退出和取消回弹已通过双端验收 |
| 本轮完成 | `VIDEO-01`～`VIDEO-06` | 播放控制、资源复用、沉浸式布局、作者页手势、操作菜单、边缘倍速与弹幕状态已完成代码和组件验证 |
| 本轮完成 | `CAPTCHA-01`～`CAPTCHA-03` | 滑动验证码的响应式缩放、触摸轨迹和服务端坐标已完成接口、组件和双端运行验证 |
| 本轮完成 | `REFRESH-01`～`REFRESH-03` | 显示模式依赖、60/90/120Hz 等时动画和 Android Surface 帧率请求已完成代码、自动化及双端验证 |
| 本轮完成 | `TABSTATE-01`～`TABSTATE-04` | 稳定标识、筛选与 Tab 持久化、懒加载边界和无效状态回退已完成自动化及双端验证 |
| 本轮完成 | `BATCH-01`～`BATCH-04` | 五类内容集合的选择、资源解析、批量入队、去重、失败提示与重试已完成自动化和 MuMu 实机验收 |
| 本轮完成 | `DYNAMIC-01` | 重构前基线巡检发现并修复动态合集卡片的窄屏 / 大字体底部溢出 |
| 本轮完成 | `REDESIGN-A` | 完成全局样式与组件审计、19 张截图矩阵、信息入口冻结、两套视觉方向比较并选定“静谧内容工作室” |
| 本轮完成 | `REDESIGN-B1` | 落地全局设计 Token、宽度分级和可访问 Entry / Section 样板，并迁移“我的”内容中心与设置总页 |

## 0. 使用规则

- `[x]`：代码已落地，并至少完成静态检查、自动化测试或设备验证中的一项有效验证。
- `[ ]`：尚未完成，或虽有实现但仍缺少关键场景验证。
- “进行中”只记录在迭代看板中；未达到完整验收条件前，正文任务仍保持 `[ ]`。
- P0：影响现有页面正确性、一致性或稳定性的回归问题。
- P1：影响核心使用体验、性能和设备适配的问题。
- P2：不阻塞现有功能的新能力。
- 每轮只勾选本轮真正完成并验证的原子任务；设备未覆盖的内容必须保留在验收记录中。
- 对齐 CloudOTP / awe_chewie 时，以可复用的组件逻辑、主题语义和交互规则为基线，不复制与 Loftify 无关的业务逻辑。
- 同一问题若同时涉及基础组件和具体页面，先修基础组件，再做页面级回归；禁止只在单个页面追加临时样式。

## 1. 已建立的设计与工程基线

后续页面必须复用这些基础实现，不再新增同用途的独立样式或交互。

- [x] `BASE-01` 浅色模式下，页面、AppBar、状态栏区域和底部导航栏使用白色背景；卡片使用浅灰层级。
- [x] `BASE-02` 统一 EntryItem、CheckboxItem、CaptionItem、InputItem、登录输入框和行内下拉菜单。
- [x] `BASE-03` 统一基础组件的圆角、图标底板、间距、分割线、选中状态和 Material 水波纹。
- [x] `BASE-04` 统一 AppBar、状态栏颜色延伸和 Cupertino 页面过渡。
- [x] `BASE-05` 统一 Toast、Dialog、BottomSheet 和 ContextMenu 的基础视觉样式；所有底部 Panel 强制裁切统一顶部圆角与安全区。
- [x] `BASE-06` 恢复全局自定义 Lottie 加载、下拉刷新和上拉加载动画。
- [x] `BASE-07` 对齐主题色、深浅色模式、字体、语言切换及本地持久化。
- [x] `BASE-08` 修复繁体中文本地化键中的不可见字符，本地化生成无缺失项。
- [x] `BASE-09` 升级 Android / Flutter 相关依赖，并完成 Android Debug APK 构建。
- [x] `BASE-10` 已在 Android 模拟器和真机完成启动、Hot Reload、Hot Restart 基线验证。

## 2. P0：现有功能回归与视觉统一

### 2.1 全局组件和页面巡检

- [x] `UI-01` 在 `UI-03`～`UI-08` 完成后执行最终扫尾：清查尚未迁移的页面、卡片、Panel、列表项和弹出菜单，移除重复实现及无必要的硬编码颜色、圆角、间距和字体。
- [x] `UI-02` 统一 Tab 的单行文字、选中状态、背景、指示器、点击反馈和自适应滚动；窄屏不得换行或挤压。
- [x] `UI-03` 统一更多菜单、长按菜单和行内菜单的图标、分割线、圆角、水波纹与安全区。
- [x] `UI-04` 逐页核对 AppBar 与状态栏颜色延伸，避免顶部白边、色块断层或返回按钮样式不一致。
- [x] `UI-05` 逐页核对浅色模式背景；页面、底部导航栏和主要滚动区域不得出现非设计性的灰底。
- [x] `UI-06` 检查深色模式、横屏、平板和桌面布局，避免局部白块、颜色断层、文字换行和控件溢出。
- [x] `UI-07` 检查主题、字体和语言切换后的即时刷新与重启恢复，避免局部仍使用旧配置。
- [x] `UI-08` 检查所有加载入口，确保使用统一 Lottie 动画，并覆盖首次加载、刷新、分页、空状态、失败和重试状态。
- [x] `L10N-01` 完成真实英文资源：716 个业务文案已完成真实英文翻译、领域词人工校对和本地化代码生成；四套资源键与占位符一致，英文页面未发现文字溢出。
- [x] `DYNAMIC-01` 修复动态页“合集”双列卡片在窄屏、长摘要、更新徽标或大字体下的底部溢出；卡片高度必须由封面与信息列中较高者自然决定，不得依赖固定高度裁切内容。

`DYNAMIC-01` 验证记录（2026-08-24）：最终视觉重构前的 MuMu 基线巡检在真实“动态 → 合集”数据中发现多张卡片出现 1～2px 底部溢出。订阅合集和猜你喜欢合集现已复用无固定高度的响应式卡片框架，以 100px 封面作为最小高度，标题、三行摘要、标签、更新徽标、操作按钮和系统大字体可推动整卡自然增长。新增 560px 标准宽度与 280px / 2 倍文字专项测试；全量 205 项测试通过，目标静态检查无 error / warning，Android / Windows Debug 构建成功。MuMu Android 12 已完成真实双列合集、Hot Reload 与 Hot Restart 验证，黄色溢出条消失且内容顺序、点击与继续阅读入口保持不变；修复前后截图保存在 `docs/redesign/baseline/android-phone-light-dynamic-overflow-before.png` 与 `docs/redesign/baseline/android-phone-light-dynamic.png`。

`UI-02` 验证记录：公共 Tab 已统一为页面背景、主题文字色、无水波纹点击反馈和主题色 UnderlineIndicator；4 个以上 Tab 或大字号环境自动横向滚动，文字固定单行。搜索、搜索结果、动态、标签详情、个人主页、系统通知、装扮和粮单页面已收口到同一组件。3 个组件测试、Android Debug 构建、模拟器 Hot Reload / Hot Restart 及搜索结果 6 Tab 点击验证均通过；本轮真机因未连接待补验。

`UI-03` 验证记录：桌面菜单已增加状态栏、导航栏、键盘等安全边界和最大宽度约束；移动端菜单支持长列表滚动、标题、分割线和底部安全区；禁用菜单不再响应点击或长按。行内下拉支持窄屏纵向排布、单行标签、Material 水波纹和上下方自适应展开，并修复控制器监听释放、语言切换后等值选中项显示旧标签及 `initialItem` 断言。8 个菜单测试与 3 个 Tab 测试通过，本轮文件无新增静态错误或警告，Android Debug APK 构建通过。Android 17 模拟器已完成 Hot Restart、Hot Reload、菜单打开态、English 切换及恢复“跟随系统”验证，运行日志未发现断言、溢出、未处理异常或销毁后更新；真机当前未连接，仍需在后续综合回归中补验。

`UI-04` 验证记录：业务页面 AppBar 已确认统一经过公共组件；普通、半透明和图片型头部会按实际合成背景自动选择状态栏图标明暗，图片头部默认使用浅色图标，返回按钮颜色同步。Android 已启用 edge-to-edge，状态栏与导航栏保持透明并跟随主题；平板横屏补齐顶部安全区，个人页移除重复 SafeArea，面板恢复和安全模式不再把浅色状态栏写死。新增 5 个 AppBar / 系统栏组件测试，连同菜单与 Tab 共 16 个通用组件测试全部通过；本轮文件静态检查、差异检查及 Android Debug APK 构建均通过。Android 17 模拟器已完成 Hot Restart、Hot Reload、浅色主页与设置页、深色主页及图片头部页面验证，状态栏无白边或断色，运行日志未发现断言、溢出、未处理异常或销毁后更新；真机当前未连接，仍需在后续综合回归中补验。

`UI-05` 验证记录：浅色主题的 Scaffold、Material 2 / Material 3 导航栏统一使用纯白页面底色，包括自定义浅色主题；`canvasColor` 与 `cardColor` 继续用于卡片、输入框、搜索框和占位图等有层级含义的组件。首页横竖屏底层、公共 `getBackground` 与主底部导航均使用页面背景；合集、粮单和帖子详情的底部操作栏已从卡片灰底迁移为页面底色。新增 4 个主题与导航背景测试，连同既有 AppBar、菜单和 Tab 共 20 个通用组件测试全部通过，差异检查及 Android Debug APK 构建通过；相关页面文件仅保留既有静态提示，本轮未新增问题。Android 17 模拟器已完成 Hot Restart、Hot Reload、首页 / 搜索 / 动态 / 我的、设置、柔和光明主题、极简白恢复及粮单详情底栏验证，页面、主滚动区、底部导航和手势区均连续为白色，运行日志未发现断言、溢出、未处理异常或销毁后更新；真机当前未连接，仍需在后续综合回归中补验。

`UI-06` 验证记录：移动端方向策略现在会在窗口尺寸或显示密度变化后重新计算；手机继续锁定竖屏，达到 `600×900dp` 平板断点后主动解除方向锁，修复由手机尺寸切换到平板横屏时应用仅占左侧、右侧出现大面积黑区的问题。桌面启动和运行期统一采用公共 `800×640` 最小窗口约束，消除首次启动、窗口缩放和恢复尺寸之间的限制不一致；帖子详情的双栏阈值已改为独立命名，避免再被误用为窗口约束。另修复平板 / 桌面侧栏主题按钮只显示水波纹却不执行回调的问题。新增 3 个响应式断点测试和 1 个动态工具按钮测试，连同既有主题、加载、AppBar、菜单和 Tab 共 27 个通用组件测试全部通过；目标文件静态检查无 warning 或 error，仅保留既有 info 提示，差异检查通过。Android Debug APK 与 Windows Debug 编译 / 启动通过；Android 17 模拟器完成手机竖屏、平板竖屏 / 横屏、深浅色、首页 / 搜索 / 动态 / 我的 / 设置、侧栏主题切换、Hot Restart 与 Hot Reload 验证，Windows 完成最小窗口下验证码登录、三列首页、深浅色侧栏切换、Hot Restart 与 Hot Reload 验证，未发现布局溢出、颜色断层或运行异常。模拟器密度、自动旋转、方向和浅色状态均已恢复；Android 真机当前未连接，仍需在后续综合回归中补验。

`UI-07` 验证记录：系统语言和系统明暗监听已上移到应用根节点，登录前、锁屏和业务页共享同一刷新链路，并同步 `Intl.defaultLocale`；语言解析优先匹配地区、再回退到语言。字体选择改为等待资源加载成功后再持久化和刷新根主题，内置字体从远程缓存加载，自定义字体从本地文件恢复，失败不会覆盖当前选择；字体设置摘要与全局页面同步更新。归档年月改用当前语言资源，并修正生成器按参数名排序后原调用传反年月的问题。新增 5 个外观状态测试，本地化测试同时加强为键、占位符及顺序一致性校验；连同既有通用组件共 35 项测试全部通过，目标静态检查零问题，Android Debug APK 构建成功。Android 17 模拟器完成浅色 / 深色、跟随系统 / 简体中文 / English、系统字体 / MiSans 的即时切换，验证统一 Lottie 字体下载进度，并确认“简体中文 + 深色 + MiSans”组合在 Hot Restart 后同时恢复；Android 与 Windows 调试会话均完成 Hot Restart 和 Hot Reload。最终模拟器已恢复“跟随系统 + 浅色 + 系统字体”，运行日志未发现布局溢出、未处理异常或销毁后更新；Android 真机当前未连接，仍需在综合回归中补验。

`UI-08` 验证记录：Lottie 加载构建器已在应用启动早期注册，并依据当前页面的实际明暗主题选择资源，避免自动锁定、主题切换或重启阶段回落为系统加载圈。首次加载、下拉刷新、分页加载、通用加载与失败重试容器、图片预览、视频下载和异步下拉搜索均接入统一动画；视频下载百分比继续保留，加载容器默认背景改为透明以延续页面底色。业务页面和公共组件的活动路径已无直接系统进度圈。新增 3 个加载动画组件测试，连同既有主题、AppBar、菜单和 Tab 共 23 个通用组件测试全部通过；目标文件静态检查无错误或警告，差异检查及 Android Debug APK 构建通过。Android 17 模拟器已完成浅色与深色下拉刷新动画、Hot Restart、Hot Reload 及浅色默认状态恢复验证，运行日志未发现断言、溢出、未处理异常或销毁后更新；真机当前未连接，仍需在后续综合回归中补验。

`L10N-01` 验证记录：英文资源的 716 个业务文案已全部转换为真实英文，并对登录、动态流、收藏夹、粮单、用户关系、设置、版权与解锁道具等领域词进行人工校对；CloudOTP 复用文案中的品牌残留已清除，仅在彩蛋的真实开发经历中保留项目名。四套 ARB 资源均为 716 个业务键，占位符及顺序完全一致；英文仅保留字体预览所需的非拉丁示例。`flutter gen-l10n` 与目标静态检查通过；新增 3 个本地化资源回归测试，连同既有通用组件测试共 30 项全部通过，Android Debug APK 构建成功。Android 17 模拟器完成“跟随系统 / 简体中文 / English”即时切换、英文重启恢复、我的页、设置页、语言行内菜单和长说明换行验证，并执行 Hot Restart 与 Hot Reload；运行日志未发现断言、溢出、未处理异常或销毁后更新，最终已恢复“跟随系统”。Windows 调试会话完成 Hot Restart 与 Hot Reload，最小窗口继续正常运行；Android 真机当前未连接，英文真机布局仍需在后续综合回归中补验。

`UI-01` 验证记录：页面层直接使用系统 AppBar、Card、ListTile、系统弹窗、系统下拉菜单和原生页面路由的残留已清零；帖子更多操作迁移到响应式 `BottomSheetBuilder`，手机使用统一底部弹层，宽屏使用统一对话框。Panel 初始页接入公共淡入路由，底部导航、Tab、次要文本、箭头、关注按钮、收藏夹标题和手势锁状态改用主题文字层级与语义色；三个搜索入口移除重复灰底并复用公共搜索栏背景。`GeneralPostItem` 独立为无页面循环依赖的数据模型，消除 Windows 分析器的重复类型边界。相关本地化、通用组件和帖子组件共 41 项测试全部通过，整组目标静态检查为 0 error（仅保留 2 个既有未使用字段 warning），差异检查及 Android Debug APK 构建通过。Android 17 模拟器完成浅色 / 深色搜索页、底部导航、帖子更多弹层、Hot Reload 与 Hot Restart 验证，Windows 调试会话同样完成 Hot Reload 与 Hot Restart；运行日志未发现布局溢出、未处理异常、销毁后更新或空值崩溃，模拟器最终恢复浅色。Android 真机当前未连接，仍需在后续综合回归中补验。

验收：同类组件在不同页面上具有一致的高度、内边距、圆角、分割线、字体和交互反馈；主题、语言、字体或窗口尺寸变化后无明显断层和溢出。

### 2.2 帖子卡片与图片排版

- [x] `POST-01` 修复纯文字帖子卡片丢失圆角的问题。
- [x] `POST-02` 统一瀑布流、Grid、图文和纯文字帖子卡片的 12px 圆角、裁切与点击边界；瀑布流保持无水波纹。
- [x] `POST-03` 优化 1～9 张图片的比例、间距和圆角，并重构 7 图、8 图拼图结构。
- [x] `POST-04` 修复帖子组件复用后仍持有旧 Item 和旧图片比例的问题。
- [x] `POST-05` 修复缺失媒体或异常图片比例导致的卡片崩溃，提供安全回退。
- [x] `POST-06` 覆盖 1～9 图、超长标题、无标题、无标签、缺失媒体、异常比例和加载失败的视觉边界测试。
- [x] `POST-07` 检查卡片在列表首尾、单列、双列、瀑布流以及布局切换后的圆角和高度稳定性。

`POST-06`、`POST-07` 验证记录：瀑布流、Grid、Tile 三类卡片统一了缺失媒体且无正文时的局部错误卡片；滚动列表中的 HTML 摘要关闭整卡文本选区，详情正文仍保留选择能力，消除了窗口尺寸变化时的 SelectionArea 布局断言。Tooltip 的全局指针监听会随组件停用和恢复正确注册，并迁移到新的主题约束模型，避免热更新或窗口变化期间创建重复动画控制器。组件测试覆盖 1～9 图四角圆角、有限尺寸、7→8 图更新、超长标题、无标题、无标签、缺失媒体、异常比例、图片失败、同 Key 数据替换以及列表首尾点击边界；本地化、通用组件和帖子组件共 49 项测试全部通过，目标静态检查零问题，Android Debug APK 构建成功。Android 17 模拟器完成单列列表、Grid、瀑布流、布局往返切换、平板多列横屏、滚动、恢复手机竖屏、Hot Reload 与 Hot Restart 验证，关键运行异常计数均为 0；截图保存在 `build/post07-feed-list.png`、`build/post07-tag-grid-loaded.png`、`build/post07-tag-waterfall-restored.png`、`build/post07-tablet-landscape-clean.png` 和 `build/post07-final-restored-clean.png`。Windows 调试会话完成 Hot Reload 与 Hot Restart；Android 真机当前未连接，仍需在后续综合回归中补验。

验收：各种帖子类型均无直角泄漏、裁切错误、溢出、高度跳变或错误复用；异常内容只影响当前卡片，不得使列表或详情页崩溃。

### 2.3 个人主页

- [x] `PROFILE-01` 重构信息层级、数据概览和操作入口。
- [x] `PROFILE-02` 使用统一的 Panel / Caption 卡片，并正确处理列表首尾圆角。
- [x] `PROFILE-03` 统一页面背景、滚动区域、吸顶区域和卡片之间的视觉衔接。
- [x] `PROFILE-04` 修复嵌套滚动、圆角裁切、回弹、刷新和吸顶手势冲突。

`PROFILE-01`～`PROFILE-03` 验证记录：个人主页重排为头像与身份摘要、元数据、更多信息、四段数据概览和全宽主操作的稳定层级；本人页面使用“在浏览器中打开”，其他用户正确显示关注状态。数据概览抽为基于 `ContainerItem` 的 `ProfileOverviewCard`，统一 14px 圆角、分隔线、语义色和 Material 水波纹，长标题保持单行省略；中日韩沿用“万”单位，其他语言使用 K / M / B 紧凑数字，避免英文大数字挤压。装扮展示同样收口到统一卡片和点击边界，并补齐稳定 Hero / Tab 标识、异步 mounted 保护及控制器释放。图片头部增加主题占位与渐变遮罩，AppBar 颜色连续扩展至状态栏；展开头部、白色内容区和吸顶 Tab 在手机竖屏、平板横屏与折叠滚动状态下无颜色断层。新增 1 个个人主页组件测试；本地化、通用组件、帖子组件和个人主页共 50 项测试全部通过，本轮 3 个目标文件静态检查零问题，Android Debug APK 构建成功；全仓库分析仍保留 2716 项旧代码与内置第三方目录的既存提示。Android 17 模拟器完成本人 / 其他用户、关注态、超大统计数字、手机竖屏、平板横屏、吸顶滚动、Hot Reload 与 Hot Restart 验证，关键运行异常计数均为 0；截图保存在 `build/profile-own-portrait.png`、`build/profile-own-collapsed.png`、`build/profile-tablet-landscape.png`、`build/profile-tablet-landscape-collapsed.png` 和 `build/profile-other-compact.png`。Windows 调试会话完成 Hot Reload 与 Hot Restart；Android 真机当前未连接，仍需在后续综合回归中补验。

`PROFILE-04` 验证记录：个人主页的四个内容 Tab 改为单一嵌套滚动链，子列表直接复用主滚动控制器，嵌套场景不再同时执行手动首次刷新与 `refreshOnStart`；分页只保留 EasyRefresh 入口，并补齐控制器释放和无更多数据判断。Android 17 模拟器完成资料头展开 / 折叠、吸顶 Tab、连续快速滑动及“文章 / 喜欢 / 推荐 / 合集”往返切换；“喜欢”和“推荐”首次进入各仅发起 1 次请求，未发现跳位、重复加载或布局异常。截图保存在 `build/scroll-profile-loaded.png`、`build/scroll-profile-collapsed.png` 和 `build/scroll-profile-tabs.png`；Android 真机当前未连接，仍需补验物理触控与高刷新率回弹。

验收：连续滚动无卡顿、错位和异常裁切；刷新、吸顶、Tab 切换与返回行为稳定；浅色模式背景连续为白色。

### 2.4 帖子详情与评论区

- [x] `DETAIL-01` 重构评论区的层级、头像、正文、时间、操作按钮和多级回复布局。
- [x] `DETAIL-02` 优化底部操作栏的安全区、按钮间距、键盘弹出和输入状态。
- [x] `DETAIL-03` 统一文章、图片、视频和合集详情页的 AppBar、页面背景与过渡效果。
- [x] `DETAIL-04` 修复文章详情解析失败时的整页异常，提供局部错误提示和重试入口。
- [x] `DETAIL-05` 排查并优化文章详情滚动掉帧、内容重排和高度跳变。

`DETAIL-01`～`DETAIL-04` 验证记录：一级评论与多级回复统一为可复用 `CommentItem` 卡片，头像、作者与回复关系、作者/置顶标识、正文、时间地点和点赞操作使用同一层级规则；评论列表移除嵌套 `ListView`，弹层支持刷新、分页、局部失败重试和底部安全区。文章详情、合集与粮单底栏统一为白色 `DetailBottomBar`，四个文章动作等宽分配，长英文标签不再粘连或越界；正文不再延伸到底栏背后，评论跳转和弹层输入环境会随系统安全区与键盘可用高度调整。文章核心数据会先独立完成渲染，礼物、评论等辅助请求失败不再拖垮整页；正文 HTML 解析异常收口为内容区局部提示与重试入口，宽松数值、布尔值、空评论作者等服务端数据也增加安全解析。文章、图片、视频、合集与粮单继续复用公共 AppBar、语义背景和 Cupertino 过渡，合集/粮单吸顶区改为稳定标识；视频页保留符合媒体场景的黑色沉浸式系统栏。新增 5 个详情组件与模型回归测试，连同本地化、通用组件、帖子组件和个人主页共 55 项测试全部通过；详情相关 10 组路径静态检查为 0 error、0 warning、0 info，Android Debug APK 构建成功。Android 17 模拟器完成手机竖屏、平板密度、正文滚动、评论定位、全量评论弹层、Hot Reload 与 Hot Restart 验证，关键运行异常计数均为 0；截图保存在 `build/detail-current.png`、`build/detail-tablet.png`、`build/detail-tablet-comments.png` 和 `build/detail-tablet-sheet.png`。Windows 调试会话完成 Hot Reload 与 Hot Restart；Android 真机当前未连接，本轮详情页真机键盘、安全区与物理横屏仍需在综合回归中补验。

`DETAIL-05` 验证记录：文章正文的 HTML / 纯文本提取从 `build` 移至内容变化时执行，解析结果和渲染组件会跨父级无关刷新复用；正文区增加独立重绘边界，手机与桌面详情的整页大 `Column` 改为按段构建的 Sliver / List 子项。推荐分页只在请求成功后推进页码，空页与全重复页会正确结束加载，移除滚动监听和 EasyRefresh 的双重触发。新增正文解析缓存回归测试；Android 17 模拟器对长文章连续执行 6 次快速滑动，正文、彩蛋区和合集导航均无高度跳变，未处理异常、布局溢出、延迟初始化和销毁后更新计数均为 0；截图保存在 `build/long-article-top.png` 和 `build/long-article-fast-scroll.png`。

`DETAIL-05` 补充回归（待真机手感确认）：帖子详情底部操作改为右下角白底竖向悬浮条，带边框、阴影及向右进出动画；向下滚动隐藏、向上滚动恢复。合集 / 粮单、标签、点赞推荐操作区或评论区任一进入视窗时会优先隐藏。显隐状态通过独立监听器只重建悬浮条，不再刷新长文章列表；视窗检测分别挂在原有列表子项上，不改变评论跳转后的 Sliver 层级与滚动锚点，用于修复跳到评论后向上滚动瞬间回到文章顶部的问题。Android 模拟器与 Android 16 真机已完成 Hot Reload，仍待物理触控复测后关闭本条。

验收：长文本、多级回复、键盘弹出、横屏及解析失败时无溢出或整页崩溃；底部操作栏不遮挡内容，返回后列表状态保持正确。

### 2.5 Lottie 刷新动画与复杂滚动

- [x] `SCROLL-01` 调整 Lottie 下拉动画层级，使动画与列表连续移动，不被列表内容覆盖。
- [x] `SCROLL-02` 处理帖子详情、个人主页、标签详情和合集 Panel 的嵌套滚动冲突。
- [x] `SCROLL-03` 统一刷新触发阈值、回弹距离、加载状态和手势优先级。
- [x] `SCROLL-04` 避免重复刷新、刷新卡住、快速滑动误触发及动画提前消失。
- [x] `SCROLL-05` 缩小上下拉 Lottie，按下拉进度从小到大显现并让列表同步让位；分页在距底部约一屏内自动预取，无需拖到底部。

`SCROLL-01`～`SCROLL-05` 验证记录：公共 Lottie Header / Footer 统一为内容上方层级、受控触发距离、有限回弹和弹簧复位；动画尺寸缩小到 32～36px，轻拉时按进度从小到大、由淡到实显现，列表同步让出真实空间。Footer 在距底部 240px 时自动预取下一页，请求进行中不会重复触发，保留触底动画作为加载状态而非唯一触发入口。个人主页、标签详情、合集 / 粮单详情和合集选择弹层移除重复的 `LoadMoreNotification`、固定高度内层列表及双重首次刷新。标签瀑布流降低超大缓存范围并改为按需构建；合集 / 粮单详情改为单个 `CustomScrollView`，并新增公共 `SliverEmptyPlaceholder`，修复空态列表请求 Viewport 固有尺寸导致整页空白的问题。刷新、预取、Panel 和视频合并回归共 28 项全部通过；目标静态检查无 error / warning，Android Debug APK 构建成功，模拟器与真机均完成 Hot Reload 和 Hot Restart，运行日志无 Flutter 未处理异常。

验收：下拉和分页动画完整可见；快速、连续或取消手势不会重复发起请求，也不会留下无法结束的加载状态。

## 3. P1：稳定性、性能与设备适配

### 3.1 列表 / Grid / 瀑布流

- [x] `LIST-01` 统一普通帖子流、Grid 和瀑布流的公共列表逻辑，减少重复状态管理。
- [x] `LIST-02` 修复滚动时的错位、闪烁、重复布局和高度跳变。
- [x] `LIST-03` 优化大量帖子下的构建次数、图片解码、缓存、内存占用和组件复用。
- [x] `LIST-04` 检查刷新、分页、返回页面和切换布局后的滚动位置恢复。
- [x] `LIST-05` 对单条内容解析失败提供卡片级回退，避免整列或整页渲染失败。

验收：长列表持续滚动无明显掉帧或闪烁；分页无重复内容；单条异常不影响其他内容；返回页面时位置和已加载内容保持稳定。

`LIST-01`～`LIST-05` 验证记录：首页、搜索六类结果和动态四个页签统一接入公共分页控制器，刷新会失效旧请求，游标仅在成功后提交，相邻页按业务 ID 去重，辅助元数据使用统一合并策略；单条解析失败只跳过异常卡片。搜索输入与建议请求加入代次检查，快速改词不会让旧响应覆盖新结果。普通流、Grid、瀑布流和日期分组均使用 Builder / Sliver 懒构建、稳定 Key 和一屏预取，收藏夹详情移除嵌套完整 Grid，所有 `cacheExtent: 9999` 已清零；帖子卡片与九宫格不再永久保活离屏状态。网络缩略图按控件宽度和设备像素密度解码，单边约束保留源图比例，最大解码边为 4096；SVG 类型缓存限制为 200 项，常见带查询参数的图片不再额外发送 HEAD 请求。头像、封面和帖子缩略图显式使用 `BoxFit.cover`，头像框、透明装扮部件和徽标显式使用 `BoxFit.contain`，避免依赖默认值或发生挤压变形。72 项自动化测试通过，相关静态检查无 error / warning。模拟器完成搜索深度滚动、All → Article → All 与动态 Follow → Tag → Follow 位置恢复；Android 模拟器和真机均完成 Hot Reload、Hot Restart 与图片长列表浏览，真机连续浏览前后 PSS 从 707277 KB 降至 695060 KB，未出现持续增长，关键运行异常计数为 0。验收截图保存在 `build/list-p2-search-result.png`、`build/list-p2-search-restored.png`、`build/list-p2-dynamic-restored.png`、`build/real-device-start.png` 与 `build/real-device-aspect-fixed.png`。

### 3.2 “我的喜欢”日期分组 Grid

- [x] `LIKES-01` 统一“日期分组 Header + Grid”的布局、Key 和复用逻辑。
- [x] `LIKES-02` 修复刷新、取消喜欢和批量增删后的旧数据、空分组与布局异常。
- [x] `LIKES-03` 保证日期分组、条目数量与服务端结果同步。

验收：取消喜欢后条目立即消失，空日期分组自动移除；刷新后不恢复旧条目，也不产生重复内容。

`LIKES-01`～`LIKES-03` 验证记录：“我的喜欢”从外层 `ListView` 嵌套多个完整 `GridView` 改为单一 `CustomScrollView`，日期 Header 与 `SliverGrid` 按可视范围懒构建，帖子使用稳定 ID Key。分页偏移与本地条目数量解耦，刷新替换旧数据，分页去重；从卡片或详情页取消喜欢后会回传状态并立即移除条目，同步减少服务端总数和对应月份计数，空月份自动删除，批量清理失效内容复用同一逻辑。月份解析不再用重复计数值反查索引，保证相同月计数仍落在真实月份；新增归档排序、重复计数、批量移除、游标失败回滚、分页去重和监听释放测试。全量 66 项测试通过，相关静态检查 0 error、0 warning（仅保留既有类型标注 info），最终 Android Debug APK 构建成功。Android 17 模拟器完成多年份 Grid 初载、深度滚动、动态页签位置恢复、Hot Restart 与两轮 Hot Reload，关键运行异常计数为 0；截图保存在 `build/list-p1-home.png`、`build/list-p1-dynamic.png`、`build/list-p1-follow-restored.png`、`build/list-p1-likes.png` 和 `build/list-p1-likes-scrolled.png`。Windows 调试会话完成 Hot Restart 与 Hot Reload；Android 真机仍未连接，取消喜欢的线上写操作为避免修改账号数据未在设备上实际触发，已由状态与归档单元测试覆盖，真机物理触控和高刷新率体验仍需补验。

### 3.3 图片加载与灯箱

- [x] `IMAGE-01` 修复图片失败重试在组件销毁后继续更新状态导致的崩溃，并限制自动重试节奏。
- [x] `IMAGE-02` 分析图片加载链路，优化缩略图、原图、预加载、缓存和占位图策略。
- [x] `IMAGE-03` 优化灯箱打开、关闭、左右翻页和缩放体验。
- [x] `IMAGE-04` 支持向下拖拽退出，并同步位移、缩放、透明度和背景变化。
- [x] `IMAGE-05` 完善取消手势后的回弹，解决拖拽退出与缩放、翻页手势冲突。

验收：缩略图优先显示且不重复闪烁；失败可重试；拖拽、缩放和翻页互不误触；退出动画连续无跳帧。

`IMAGE-01`～`IMAGE-05` 验证记录：帖子列表继续按显示尺寸请求缩略图，灯箱使用去除裁切参数后的原图地址；打开灯箱后仅预热当前页前后各一张原图到磁盘缓存，避免把整组大图同时解码进内存。灯箱与公共图片加载均保留自定义 Lottie 占位，原图失败显示本地化错误和手动重试入口，切换图片时使用无缝播放避免空白闪烁。灯箱补齐分页、缩放状态和图片控制器的完整释放，修复主色异步回写顺序及下载状态延迟回调在退出后继续工作的生命周期问题。手机端照片路由改为透明全屏层；单指向下拖动会同步更新内容位移、缩放、透明度与背景遮罩，达到距离或速度阈值后连续退出，未达到阈值则使用弹性曲线回到原位。横向动作优先交给分页，双指或双击缩放状态禁止触发退出，多指加入会立即取消下拉候选，避免缩放、翻页与退出互相误触；快速甩动另设 72 逻辑像素的最低位移，防止短距离手势意外关闭。新增灯箱阈值、视觉进度、透明路由和缩略地址解析测试，全量 77 项自动化测试通过；灯箱核心文件静态检查零问题，扩展业务入口无新增 error / warning（仅旧业务文件保留既有 info），Android Debug APK 构建成功。Android 17 模拟器和 Android 真机均完成原图加载、Lottie 占位、短拉回弹、长拉退出；真机额外完成三图左右翻页、双击放大后拖动不退出、恢复缩放后退出，以及双端 Hot Reload / Hot Restart。关键运行异常计数为 0；验收截图保存在 `build/image-viewer-emulator-short-rebound.png`、`build/image-viewer-emulator-drag-midpoint.png`、`build/image-viewer-emulator-long-dismiss.png`、`build/image-viewer-real-short-rebound.png`、`build/image-viewer-real-zoomed-drag.png` 和 `build/image-viewer-real-long-dismiss.png`。

### 3.4 视频播放

- [x] `VIDEO-01` 系统排查播放、暂停、进度拖动、全屏、横竖屏、静音和后台切换。
- [x] `VIDEO-02` 修复列表复用、自动播放、资源释放和多个播放器同时工作的异常。
- [x] `VIDEO-03` 统一加载、错误、重试和下载状态的视觉反馈。
- [x] `VIDEO-04` 优化沉浸式进度条、横屏排版、底部文字区、操作数字缩写和作者主页滑动过渡。
- [x] `VIDEO-05` 统一长按操作面板，持久化自动连播与弹幕、保持列表内播放速度，并保证作者页系统返回只关闭作者页。
- [x] `VIDEO-06` 在视频左右边缘长按临时 2x 播放、松手恢复原倍速；弹幕按安全轨道错峰排列，播放暂停时同步冻结并从原位置恢复。

验收：重复进入退出、切换前后台和旋转屏幕后播放器状态正确，无黑屏、串音、重复播放或资源泄漏。

`VIDEO-01`～`VIDEO-06` 验证记录：视频列表仅允许当前页播放器继续播放，页面复用、初始化失败、重试和释放均收口资源；自动连播切换到下一条时不再因作者组件 Key 变化重建列表并闪回第一条。进度条改为沉浸式样式，长按后扩大触控区并支持连续拖动；全屏横屏重新排列底部文字、作者头像和右侧动作，关注后的头像不再显示重复状态标记，点赞等大数值使用 `2.9w` 一类单行缩写。左滑作者主页使用跟手动画，返回时由页面本地历史优先收回作者层，系统返回手势不再退出视频页。长按菜单移除 Grid 默认边距造成的空白，统一圆角、按钮、选中态与播放速度区；自动连播和弹幕写入本地配置，Hot Restart 后仍恢复，倍速在当前视频列表内切换页面时保持。新增左右边缘 16%（52～92dp）临时 2x 长按区，松手或取消时恢复原倍速；弹幕使用 4～5 条安全轨道与错峰时间槽，暂停后冻结当前位置。视频控制器测试 12 项全部通过，边缘倍速、同轨避让和暂停同步已有独立回归覆盖；Android 模拟器和真机均已运行本轮 Debug 构建并完成 Hot Reload / Hot Restart，未发现连接丢失以外的 Flutter 运行异常。

### 3.5 合集 / 粮单上下篇切换

- [x] `SWIPE-01` 在详情页支持左右滑动切换合集或粮单的上一篇、下一篇。
- [x] `SWIPE-02` 设计边界阻尼、切换阈值、方向提示和页面过渡动画。
- [x] `SWIPE-03` 解决横向切换与正文纵向滚动、图片灯箱手势之间的冲突。
- [x] `SWIPE-04` 预加载相邻内容，并处理首篇、末篇和加载失败状态。

验收：只有达到明确阈值才触发切换；首末边界有清晰反馈；切换后标题、正文和操作状态对应正确内容。

`SWIPE-01`～`SWIPE-04` 完成记录（2026-08-24）：合集详情与粮单阅读序列支持左右拖拽切换、边界阻尼、页面跟手位移、切换加载态、相邻内容预取及失败回退；没有合集 / 粮单上下文的独立帖子只保留阻尼回弹，不显示“第一篇 / 最后一篇”文字或松手提示。首末篇文字、轻震和松手反馈统一使用正常切换上下篇的动态提交距离，边界提示与上下篇提示复用同一缩放淡入容器。新增手势竞技场分流：多图区域中部横滑只翻图，屏幕左右 28dp 边缘稳定负责切篇；边缘手势会优先于长文 `SelectionArea`，正文中部仍可选择文字，纵向滚动继续交给阅读列表，灯箱独立路由不受详情页手势影响。MuMu Android 12 已验证两图帖子从 1/2 翻到 2/2 而不误切文章、公开粮单中上一篇 / 下一篇双向连续切换、公开合集 43/44 → 42/44 → 43/44 → 44/44，以及末篇继续拖拽只回弹不越界；每次切换后标题、作者、正文、合集序号和互动数据对应新帖子。15 项手势 / 序列专项测试及 195 项全量测试通过，新增文件静态检查零问题，Android/Windows Debug 构建成功，MuMu Hot Reload / Hot Restart 无编译或框架异常；独立 Android 真机当前未连接，仍保留后续综合触感复核项。

### 3.6 滑动验证块

- [x] `CAPTCHA-01` 修复不同屏幕尺寸、分辨率和显示缩放下的位置与尺寸误差。
- [x] `CAPTCHA-02` 覆盖小屏、平板、横屏和高 DPI 场景。
- [x] `CAPTCHA-03` 确保滑块轨迹、触控范围、图片缩放和实际验证距离一致。

`CAPTCHA-01`～`CAPTCHA-03` 验证记录：验证码会先解码服务端背景图和拼图层的真实像素尺寸，再以统一进度分别换算滑块逻辑轨迹、拼图显示轨迹和服务端原图 `xPos`，移除固定 `1.1` 缩放及“控件位移直接当原图坐标”的误差。真实接口抽样返回背景 `320×160`、拼图层 `82×160`，现实现会随每次挑战的真实尺寸计算，不依赖固定素材规格；宽屏不放大原图，窄屏保持比例缩小，安全区和低高度使用可滚动布局。滑块从按下位置开始累计拖动，首段触控 slop 不再造成手指、滑块和提交坐标错位；请求代次、图片解码资源、延迟状态和退出后的异步回写也已收口。6 项几何与组件测试覆盖 280px 小屏、4×DPI 手机、1024×600 横屏平板、极端约束、轨迹端点和实际提交坐标，全部通过；目标静态检查零问题，Android Debug APK 构建成功，模拟器与真机均完成 Hot Reload / Hot Restart，运行日志无 Flutter 异常。

验收：各设备上的视觉缺口、滑块终点和提交坐标一致，无明显偏移或触控死区。

### 3.7 高刷新率设备

- [x] `REFRESH-01` 排查 90Hz、120Hz 及更高刷新率下的动画和滑动问题。
- [x] `REFRESH-02` 清除依赖固定 60Hz 的帧率、速度或动画时长计算。
- [x] `REFRESH-03` 优化列表、灯箱、Lottie、页面过渡和手势动画的流畅度。

`REFRESH-01`～`REFRESH-03` 验证记录：`flutter_displaymode` 与本地 `awesome_chewie` 已同步升级到 `0.7.0`；显示模式使用稳定签名持久化，并允许 60 与 59.94Hz 等同一物理模式的微小浮点漂移，避免系统升级后把用户保存的 60Hz 误恢复成最高刷新率。Android 除设置 `preferredDisplayModeId` 外，会在 Android 11+ 对 Flutter 主 Surface 设置精确刷新率提示，低版本仍使用窗口 `preferredRefreshRate`，跟随系统时用 0 清除提示，应用恢复前台会重新应用。全仓动画与滑动计算未发现按“每帧固定增量”或固定 60Hz 推进的业务路径；页面过渡、弹幕、Lottie、灯箱和刷新均基于真实经过时长或手势逻辑距离。新增 60/90/120Hz 等时页面过渡与弹幕位移回归，连同显示模式、加载、灯箱和视频共 34 项测试全部通过；目标静态检查零问题，Android Debug APK 构建成功，模拟器与真机均完成完整安装、Hot Reload 和 Hot Restart。真机窗口与系统 App Request 均确认请求 120Hz，但该 MIUI 系统同时施加 `PRIORITY_MIUI_REFRESH_RATE` 最大 60Hz、`mIgnorePreferredRefreshRate=true` 的厂商策略，因此本机最终活动模式仍为 60Hz；应用侧请求链路有效，实际 90/120Hz 物理显示仍需在未对应用限帧的设备上复核。

验收：60Hz、90Hz 和 120Hz 下动画速度一致，无异常加速、减速、周期抖动或显著掉帧。

### 3.8 标签页状态记忆

- [x] `TABSTATE-01` 保存标签页最后使用的筛选、排序和 Tab。
- [x] `TABSTATE-02` 再次进入时恢复选择、列表内容和必要的滚动位置。
- [x] `TABSTATE-03` 明确页面生命周期内记忆与 App 重启后持久化的边界。
- [x] `TABSTATE-04` 数据源变化或保存状态失效时安全回退到默认值。

`TABSTATE-01`～`TABSTATE-04` 验证记录：搜索榜单、搜索结果、动态页、标签详情、标签合集 / 粮单、个人主页、系统通知和装扮页均改用稳定业务标识保存选中项，数据源重排、翻译变化或旧索引失效时会安全回退并迁移旧状态。标签详情额外持久化最新 / 最热筛选参数；用户可见选择跨 App 重启恢复，已加载响应和滚动位置只保留在当前页面生命周期，避免恢复陈旧列表。各 Tab 首次进入按需加载，失败后允许重试；Tab 文字字号、字重和颜色跟随指示器进度连续过渡。横向手势刚指向相邻页就登记预加载，不再等待滑到中点或切页结束；标签详情与搜索结果会等待目标 EasyRefresh 获得实际纵向滚动位置后立即拉出 Lottie 刷新头并执行同一请求，首次进入、重复点击当前 Tab 与筛选变更不再静默请求。搜索结果 6 Tab 与个人主页 Tab 按整行等宽分布。个人主页撤销外层手势遥控刷新，改为各子列表使用 34px 触发阈值的原生 Lottie 刷新头，刷新结束由同一状态机管理；正文顶部通过实际裁剪保留 20px 圆角，避免被 TabBar 的不透明 Material 覆盖。作品、推荐、合集和粮单的空数据视图使用完整非收缩视口，避免占位被裁切。粉丝、关注及搜索用户列表迁移为懒构建圆角用户卡片，统一头像、昵称、ID、简介、关系按钮、边框与主题点击反馈，并移除重复触底入口。新增等宽 6 Tab 与 320px 长文本用户卡片回归；全量 125 项测试通过，目标静态检查无新增 error / warning，Android Debug APK 构建成功。Android 17 模拟器与 Android 16 真机均完成 Hot Reload、Hot Restart，重启后的新日志未发现未处理异常或布局溢出。

验收：返回标签页时选择不丢失；恢复过程不重复请求或展示错误筛选结果；无效状态可自动降级。

## 4. P2：新交互与下载能力

### 4.1 划线评论

- [ ] `ANNOTATION-01` 支持选中正文文本并针对选区发表评论。
- [ ] `ANNOTATION-02` 建立评论与原文文本区间、内容版本之间的关联。
- [ ] `ANNOTATION-03` 设计划线高亮、评论展示和点击评论定位原文的交互。
- [ ] `ANNOTATION-04` 处理原文修改、选区失效、重叠划线和跨段选择。

验收：评论能够稳定定位原文；原文变化后不会错误绑定其他内容，并有明确的失效提示。

### 4.2 批量下载

- [x] `BATCH-01` 支持批量下载“我的喜欢”“我的推荐”“我的收藏”。
- [x] `BATCH-02` 支持批量下载合集、粮单等内容集合。
- [x] `BATCH-03` 支持全选、部分选择、下载进度、失败提示和失败重试。
- [x] `BATCH-04` 对重复资源去重，并明确已下载文件的覆盖或跳过规则。

`BATCH-01`～`BATCH-04` 完成记录（2026-08-24）：我的喜欢、我的推荐、我的收藏、合集和粮单均接入同一批量下载页；部分选择可即时调整，全选会安全遍历全部分页并在数据不再增长时停止。资源解析覆盖原图、文章正文图片和视频源，单篇解析失败不会阻断其余内容；解析及入队进度、失败数量、重试入口和下载管理入口均有明确反馈。批次内与已有任务按资源地址去重：等待中、下载中和已完成任务明确跳过，失败、暂停或取消任务重置后重新排队，不覆盖已下载文件。资源解析、任务队列和 320×568 窄屏交互均有专项回归，全量 203 项测试通过，Android / Windows Debug 构建成功。MuMu Android 12 已完成 Hot Reload、Hot Restart、部分选择、全选和真实资源入队验证；一次真实批次成功加入 7 项、跳过 0 项，并正确报告 1 篇无法下载，下载管理页同步显示任务进度和控制操作。

验收：选中结果与任务数量一致；部分任务失败不影响其他任务；重复内容不会重复下载。

### 4.3 下载任务系统

- [x] `DOWNLOAD-01` 建立统一下载任务模型和管理页面。
- [x] `DOWNLOAD-02` 展示等待中、下载中、已完成、失败和已取消状态。
- [x] `DOWNLOAD-03` 支持暂停、继续、取消、重试和清理记录。
- [x] `DOWNLOAD-04` 处理并发数量、后台下载、网络切换和断点续传。
- [x] `DOWNLOAD-05` 处理文件命名、重复文件、临时文件清理和空间不足。

进展（2026-08-24）：统一任务底座已覆盖图片、视频和通用文件入口，支持任务持久化、并发队列、暂停 / 继续 / 取消 / 重试、失败恢复、断点文件保留、重复 URL 去重、文件名冲突递增、临时文件清理和可读错误提示；Android 已接入 WorkManager 系统后台队列和前台下载通知，网络类失败会在网络恢复后自动重排队，且不会擅自恢复用户手动暂停的任务。下载管理已作为用户内容资产入口加入“我的 → 内容中心”，原图片设置内的快捷入口继续保留，两处复用同一管理页面和任务状态。MuMu Android 12 已用真实帖子完成图片和视频下载验收：飞行模式下系统任务保持 `CONNECTIVITY` 等待约束，应用退到后台并恢复网络后出现 Loftify 下载通知，任务继续并将 4,259,137 字节视频写入系统 Movies 目录，返回应用后记录为 100% 完成；随后通过 hot restart / hot reload 确认状态持久化和运行稳定。

验收：App 重启后任务状态可恢复；网络中断后可继续；取消任务不残留临时文件；文件冲突有确定规则。

### 4.4 全局 Lucide 图标迁移

- [x] `ICON-01` 全仓盘点静态界面图标并建立语义映射表，覆盖业务代码、公共组件和本地 `awesome_chewie`；Lottie 动画、产品品牌标识和用户内容不作为普通界面图标替换。详见 [`Icon_Migration_Map.md`](Icon_Migration_Map.md)。
- [x] `ICON-02` 建立统一的 Lucide 图标组件与规范，收口默认尺寸、视觉尺寸、颜色、点击区域、基线对齐和禁用状态，禁止页面继续直接混用 Material、Cupertino 或其他图标集。
- [x] `ICON-03` 制定面性图标处理规则：默认使用 Lucide 线性图标；选中态优先通过主题色、字重感、浅色底板、圆点或容器形态表达，不用另一套面性图标破坏一致性；确有轮廓识别不足的极少数场景，使用基于同一网格的本地配对 SVG，并登记为受控例外。
- [x] `ICON-04` 分阶段迁移导航、AppBar、列表项、设置、菜单、对话框、空状态、帖子操作、媒体控件和下载状态；Lottie 动画保留，但其静态占位、失败和无障碍替代图标仍使用 Lucide。
- [x] `ICON-05` 删除可替换的旧图标依赖和重复资源，补充语义、尺寸、选中态、深浅色、高对比度及中英文布局回归，确保图标含义、可点击范围和业务状态不因迁移改变。

进展（2026-08-24）：已完成业务层、公共组件和本地 `awesome_chewie` 的全量静态盘点，共登记 315 处 Material 引用（跨范围 146 个不同符号）、4 处 Cupertino、99 处现有 Lucide 和 51 张旧 PNG 界面图标；映射表逐项覆盖全部遗留符号，并明确排除 40 个 Lottie、Logo、插画、用户内容和远程 SVG。后续迁移以 [`Icon_Migration_Map.md`](Icon_Migration_Map.md) 为唯一语义基线，新增页面不得扩大遗留数量。已新增 `ChewieIconThemeData`、`ChewieIcon`、`ChewieIconButton` 和业务语义层 `LoftifyIcons`，统一 16/20/24 视觉尺寸、至少 44×44 点击区域、主题色选中态、38% 禁用态、光学对齐及明暗主题状态；业务页面直接导入 Lucide 字体已由组件测试拦截，设置导航、EntryItem 与下载管理作为首批接入样例已在 MuMu 的明暗主题中通过验证。面性状态已明确为“同一语义、同一字形”，只通过主题色、底板或圆点表达；动作语义发生变化时才允许切换 Lucide 字形。当前没有受控配对 SVG 例外，测试会阻止视觉变体命名和未登记 SVG 资源进入业务层。

`ICON-04` 分批进展（2026-08-24）：全局导航与 AppBar 第一批已完成。桌面/平板侧栏、移动底栏、头像菜单、装扮、通知、设置入口和普通/Sliver AppBar 返回按钮已切换到统一 Lucide 语义；导航选中前后保留同一字形，只通过主题色和容器状态变化，AppBar 返回操作统一为至少 44×44 点击区域。相关静态守卫会阻止全局导航重新引入 Material 图标、旧 PNG 图标或选中态换图；MuMu 横屏浅色模式已完成 Hot Restart、页面切换和嵌套设置页验证。后续继续迁移列表项、菜单、对话框、空状态、内容操作与媒体控件，完成全部批次后再勾选 `ICON-04`。

`ICON-04` 分批进展（2026-08-24）：`awesome_chewie` 基础控件第二批已完成。新增组件语义层 `ChewieIcons`，并迁移 AppBar、窗口按钮、评分、数字输入、下拉搜索、右键/底部菜单、图片失败与重试、展开箭头以及 EasyRefresh/Lottie 刷新兜底状态；复用组件运行时代码已无 Material/Cupertino 图标引用，文档示例不计入运行时。评分选中态和置顶状态保持同一 Lucide 字形，反馈只通过颜色或旋转变化；自动化扫描会阻止基础组件重新混入旧图标集。下一批继续处理 Loftify 业务列表、设置、菜单、对话框和空状态。

`ICON-04` 分批进展（2026-08-24）：设置与登录输入第三批已完成。所有 `Screens/Setting` 页面、设置管理 Item 和 `LoginInputItem` 已改用 `LoftifyIcons` 语义，覆盖设置分类、关于/外链、主题重置、文件名保存/刷新、屏蔽标签增删以及输入框清空/密码显隐；登录输入尾部操作同时补齐统一 20px 字形和至少 44×44 点击区域。静态守卫会阻止这些范围重新引入 Material/Cupertino 图标，MuMu 深色横屏已验证设置首页和关于页的图标层级、外链标识与页面跳转。下一批继续迁移通用业务 Item、内容卡片操作、菜单与对话框。

`ICON-04` 分批进展（2026-08-24）：通用业务 Item 与内容卡片操作第四批已完成。`ItemBuilder`、`LoftifyItemBuilder` 的 Entry 默认图标、标签类型、搜索入口、标题尾部操作、评论展开、标签搜索以及点赞/推荐按钮已统一使用业务 Lucide 语义；搜索按钮补齐至少 44×44 点击区域，点赞和推荐的选中前后保持同一字形并仅通过状态色表达。旧的热度/搜索 PNG 接口图标不再由这两项基础组件加载，标签排行底纹等内容装饰继续保留。静态守卫会阻止该范围重新引入 Material/Cupertino 或已淘汰的接口位图；MuMu 深色横屏已验证热榜标签、搜索入口与首页内容卡片。下一批继续迁移业务菜单、对话框与空状态。

`ICON-04` 分批进展（2026-08-24）：业务底部面板第五批已完成。`Widgets/BottomSheet` 中的合集排序、装扮预览翻页与单张/批量下载、标签筛选重置、滑块验证码重试/关闭/刷新/拖拽提示、收藏夹选择以及通用选项勾选均已切换到 `LoftifyIcons`；排序方向通过同一图形旋转表达，收藏夹选中前后保持同一圆形字形并通过主题状态反馈。翻页、验证码关闭与刷新操作补齐至少 44×44 点击区域，旧排序 PNG 不再加载；面板内原有 Lottie 加载保持不变。静态守卫会阻止全部业务 BottomSheet 重新引入 Material/Cupertino 或旧排序位图；MuMu 深色横屏已验证筛选面板圆角、按钮层级和重置图标。下一批继续迁移业务页面菜单、对话框与空状态。

`ICON-04` 分批进展（2026-08-24）：内容管理页面菜单第六批已完成。我的推荐、我的喜欢、历史、收藏夹列表和支持者页面的更多、清理、删除、历史开关、新建/编辑收藏夹与支持者标识已统一为业务 Lucide 语义；AppBar 操作使用至少 44×44 点击区域，清空与删除菜单明确使用主题错误色，历史开关继续保持单一字形。静态守卫会阻止该范围重新引入 Material/Cupertino；MuMu 深色横屏已验证我的喜欢页面的悬浮更多按钮、菜单圆角、分割层级和红色危险操作。下一批继续迁移个人主页菜单及其他内容详情页操作。

`ICON-04` 分批进展（2026-08-24）：个人主页第七批已完成。主题背景、商店、头像框、备注、拉黑/屏蔽、复制/浏览器/分享、更多、简介展开、关注、热度明细、收藏、评论、代表作、热度与合集标识均已统一为业务 Lucide 语义；更多操作补齐至少 44×44 点击区域，关注与已关注保持同一字形，仅在拉黑动作下切换为明确的禁止语义，危险菜单继续使用红色错误态。旧合集白色 PNG 接口图标已移除，主页插画和用户内容不受影响。静态守卫会阻止个人主页重新引入 Material/Cupertino 或旧合集位图；MuMu 深色横屏已验证主页顶部圆角正文、统计区和更多菜单的图标、分组与圆角。下一批继续迁移帖子/合集/粮单/标签详情操作。

`ICON-04` 分批进展（2026-08-24）：标签、合集与粮单详情第八批已完成。详情 AppBar 更多、复制/浏览器/分享、正倒序、标签标题、筛选、标签内搜索、列表/网格切换、合集/粮单/热度角标与排行榜均已统一为业务 Lucide 语义；排序沿用同一图形旋转，布局切换仅在实际动作/模式变化时切换 `layoutList` 与 `layoutGrid`。排行榜前三名改为同一火焰图形配合递减层级和明确名次，不再加载三套热度位图；旧排序、标签、搜索、合集与粮单接口 PNG 已从这些页面移除，入口插画继续保留。静态守卫会阻止该范围重新引入旧图标；MuMu 深色横屏已验证标签页 44×44 操作区、列表/网格即时切换及合集粮单排行。下一批继续迁移帖子详情、帖子卡片与媒体操作。

`ICON-04` 分批进展（2026-08-24）：帖子信息流卡片与正文异常状态第九批已完成。瀑布流和列表卡片的无效内容、推荐来源、更多、复制、查看原文、浏览器、分享、喜欢、推荐与评论图标，以及正文渲染失败和重试状态均已统一为业务 Lucide 语义；喜欢和推荐的未选中/选中状态保持同一字形，仅通过颜色表达状态，避免面性图标跳变。静态守卫会阻止该范围重新引入 Material/Cupertino 图标。下一批继续迁移帖子详情页操作与下载状态。

`ICON-04` 分批进展（2026-08-24）：帖子详情页操作与下载状态第十批已完成。上下篇拖拽提示、图片翻页、粮单/合集、版权/创作标记/转载来源、收藏、评论、下载成功/失败、更多及复制/原文/浏览器/分享菜单均已统一为业务 Lucide 语义；收藏前后保持同一书签字形并使用主题色反馈，下载继续使用原有圆形进度条而非 Lottie，成功与失败使用主题语义色。AppBar 下载和更多操作补齐至少 44×44 点击区域，旧合集明暗 PNG 不再加载；现有喜欢/推荐 Lottie 与切篇动画保持不变。静态守卫会阻止帖子详情重新引入 Material/Cupertino 或旧合集接口位图；MuMu 深色横屏已验证详情作者栏、下载/更多操作、上下篇提示基础样式、竖向悬浮条和更多菜单。下一批继续迁移登录、导航与媒体页面的剩余图标。

`ICON-04` 分批进展（2026-08-24）：登录输入与登录方式第十一批已完成。密码、短信验证码、LOFTER ID 和邮箱登录页面的关闭、手机号、图片/短信验证码、密码、LOFTER ID、邮箱及其他登录方式入口均已统一为业务 Lucide 语义；字段图标按真实输入含义重新映射，密码字段不再误用“已验证”图标。登录输入组件原有清除、密码可见性、焦点、自动填充、提交和验证码逻辑保持不变，静态守卫会阻止登录页面重新引入 Material/Cupertino 图标。下一批继续迁移首页动态、搜索、我的页面与媒体控制图标。

`ICON-04` 分批进展（2026-08-24）：首页核心导航页面第十二批已完成。首页/动态页的刷新与回顶、动态标签与订阅状态、搜索历史清理/换一换/趋势、我的页面个人入口、内容与创作中心、退出、装扮、通知和设置图标均已统一为业务 Lucide 语义；合集订阅前后保持同一书签字形，首页深浅主题 Lottie 切换保持不变。我的页面 AppBar 操作统一为至少 44×44 点击区域，旧装扮/设置/标签接口 PNG 不再加载，静态守卫会阻止核心导航页面重新引入 Material/Cupertino 或这些旧位图；MuMu 深色横屏已验证首页刷新/回顶、我的内容入口和全局侧栏，设备强制横屏导致竖屏 AppBar 改由布局测试与双端构建覆盖。下一批继续迁移视频、装扮与其他剩余页面图标。

`ICON-04` 分批进展（2026-08-24）：视频播放与长按面板第十三批已完成。加载失败/重试、中央播放提示、返回、静音、全屏、播放选项、连播、分享、弹幕、下载、选择标记、喜欢、推荐、评论与关注入口均已统一为业务 Lucide 语义；喜欢、推荐、连播和弹幕的持久状态保持同一主字形，仅通过主题色与选择标记反馈，播放/静音/全屏只在实际动作变化时切换字形。顶部控制补齐至少 44×44 点击区域，长按面板下载中状态改为进度环，侧边下载继续显示实际百分比；共享响应式 Panel 现在按实际内容收缩并保持完整圆角，不再在横屏短菜单下方保留固定高度空白。原有视频加载动画、弹幕暂停同步、播放速度、状态记忆和横屏布局逻辑保持不变。静态守卫会阻止视频页重新引入 Material/Cupertino 图标或下载伪动画；MuMu 横屏已验证真实视频播放页、顶部/侧边控件与长按菜单的布局和交互。下一批完成装扮页面等最后一批旧图标并复核全局遗留。

`ICON-04` 完成记录（2026-08-24）：装扮预览卡片的进入提示和装扮详情下载入口已完成最后一批 Lucide 迁移，装扮卡片整体点击语义和下载按钮原有至少 44×44 点击区域保持不变；同时修复装扮详情刷新方法丢弃 `IndicatorResult` 导致刷新状态无法可靠结束的问题。自动化守卫覆盖最后两处遗留，业务运行时 `lib/` 已无 Material/Cupertino 图标引用；所有明确保留的 Lottie、品牌标识、用户内容和内容装饰均未被误替换。MuMu 横屏已验证装扮列表卡片、装扮详情加载完成及各部件下载入口。后续由 `ICON-05` 清理不再使用的旧图标依赖与重复资源，并补齐全局资源级验收。

`ICON-05` 完成记录（2026-08-24）：已删除无运行时引用的 51 张旧染色 PNG、`code.txt`/`run.py` 辅助文件、`AssetUtil` 废弃常量及 `pubspec.yaml` 资源声明，验证码几何测试改为运行时生成精确 72×72 的中性图片，不再依赖界面图标夹具。全局静态守卫会同时扫描 Loftify 与本地 `awesome_chewie` 运行时代码、资源目录和清单，阻止 Material/Cupertino 字形或 `assets/icon` 回流；深浅主题下的选中态、禁用态、44×44 点击区域与中英文语义均有回归，高对比度模式额外增强选中描边/底板和禁用前景。`cupertino_icons` 仅由 HTML 视频链路的第三方 `chewie` 包传递引入，并非可安全移除的直接界面依赖；Lottie、品牌、插画和内容装饰资源完整保留。

验收：除明确登记的 Lottie、品牌标识、用户内容和受控配对 SVG 外，界面不再混用其他图标体系；同类图标的尺寸、色彩、视觉重量、对齐和状态表达一致，迁移前后功能入口与业务语义完整保留。

### 4.5 首页毛玻璃主导航

- [x] `NAVGLASS-01` 为手机底部主导航建立可复用毛玻璃容器，统一模糊半径、透明度、背景染色、顶部边界、圆角、安全区和阴影；内容应自然延伸到导航下方但不能被操作区遮挡。
- [x] `NAVGLASS-02` 完成浅色、深色、自定义主题、纯黑 / 纯白主题下的语义适配，并为系统“降低透明度”、低性能设备和不支持模糊的平台提供无视觉断层的实色降级方案。
- [x] `NAVGLASS-03` 统一首页各导航项的 Lucide 图标、文字、选中态、未读角标、点击 / 按下反馈和页面切换动效；避免高饱和大色块、厚重阴影和水波纹噪声。
- [x] `NAVGLASS-04` 验证长列表滚动、键盘弹出、横竖屏、平板 / 桌面导航形态、系统手势区和 60/90/120Hz 性能；模糊层不得造成持续重绘、明显掉帧、文字对比不足或触控穿透。

验收：主导航在不同主题和设备上保持轻盈、现代且可读，滚动内容与玻璃层的空间关系自然，选中状态清晰但不喧宾夺主；关闭透明效果或性能降级后仍保持完整层级和可用性。

`NAVGLASS-01` 完成记录（2026-08-24）：手机主导航已替换为可复用的浮动毛玻璃容器，统一使用 18px 模糊、半透明主题 Surface、24px 圆角、0.8px 柔和边界、轻量阴影和系统手势安全区；主页面继续使用 `extendBody`，长列表会自然延伸到玻璃层后方，同时底栏自身高度与底部留白仍保护四个操作区。四个 Lucide 入口补回单行文字，选中态使用克制的主题色浅底与描边，按下仅做轻微缩放且不产生水波纹；高对比度、减少动画、辅助导航、Web 或显式关闭模糊时会自动使用完整实色 Surface。6 项专项测试覆盖 280px 宽屏幕、1.4 倍字体、安全区、模糊/实色分支、语义选中态、99+ 角标和触控回调；全量 183 项测试、Android/Windows Debug 构建均通过，MuMu 浅色搜索长列表已完成 Hot Reload、Hot Restart、页面切换和内容透过层级检查。

`NAVGLASS-02` 完成记录（2026-08-24）：玻璃底栏直接使用当前 `ColorScheme.surface/primary/onSurfaceVariant/error`，内置极简白、柔和浅色、GitHub 浅色、极简黑、蓝铁、GitHub 深色及自定义强调色均跟随主题语义，不再写死白底或青色。系统减少动态效果、辅助导航、高对比度或 iOS Reduce Motion、Web 平台会自动停用模糊；“外观 → 移动端设置”新增可持久化的“降低透明效果”开关，低性能设备可主动切换为同圆角、边界、阴影与层级的完整实色 Surface。中英文及繁体文案已同步；MuMu 实测开关即时生效、Hot Restart 后状态保持，极简黑与极简白对比度正常，最后恢复浅色玻璃模式。主题、降级、偏好接线与本地化回归连同全量 185 项测试通过，Android/Windows Debug 构建成功。

`NAVGLASS-03` 完成记录（2026-08-24）：首页、搜索、动态和我的四个入口保持同一 Lucide 字形，统一 22px 视觉尺寸、单行文字、主题色选中底板/描边与未选中前景；按下使用 90ms 轻量缩放，点击回调立即触发，不再被双击识别窗口延迟，也没有 InkWell 水波纹。页面栈继续保留四页状态，主导航切换改为 220ms `easeOutCubic` 过渡；系统减少动态效果或辅助导航时使用即时跳转。通用角标仅在业务提供真实未读数时展示，支持 1～99、99+ 截断、错误色语义与读屏原始数量，不伪造占位数字。专项回归覆盖点击即时性、按压反馈、稳定字形、未读语义和动效降级；全量 186 项测试、Android/Windows Debug 构建通过，MuMu 已完成首页/搜索/我的切换、Hot Reload 与 Hot Restart，未发现输入延迟、触控穿透或状态错位。

`NAVGLASS-04` 完成记录（2026-08-24）：手机竖屏继续使用毛玻璃底栏，平板横屏与桌面保持侧栏形态；底栏遵循系统手势安全区，并在键盘产生底部 `viewInsets` 时主动退出布局。真实长列表接入滚动方向联动，向内容深处滚动时隐藏、反向滚动时恢复；MuMu Android 12（1080×1920）已验证瀑布流内容延伸、隐藏和恢复过程。专项组件测试覆盖滚动联动、键盘避让策略、横竖屏 / 宽屏分支、安全区、底栏拦截点击而不穿透正文、玻璃层稳定后不存在持续调度帧，以及 60/90/120Hz 下相同的时间制动效；MuMu 使用实体键盘模式，无法弹出软键盘，因此软键盘实机外观留待独立真机补验，当前以真实 `viewInsets` 回归验证行为。全量 190 项测试、Android/Windows Debug 构建通过，MuMu 已完成 Hot Reload 与 Hot Restart，未发现编译错误、框架异常或明显掉帧。

## 5. 推荐执行顺序

1. `LIST-*`、`LIKES-*`、`IMAGE-*`、`VIDEO-*`：稳定列表与媒体体验。
2. `CAPTCHA-*`、`REFRESH-*`、`TABSTATE-*`：完成设备适配和状态恢复。
3. `SWIPE-*`：完成合集 / 粮单切篇及手势回归。
4. `DOWNLOAD-*`：先建立统一下载任务底座。
5. `BATCH-*`：复用下载任务底座实现各业务入口的批量下载。
6. `ICON-*`：完成全局 Lucide 图标语义映射、面性状态策略和分阶段迁移。
7. `NAVGLASS-*`：在统一图标体系上升级首页主导航，并完成模糊性能与降级验收。
8. `ANNOTATION-*`：依赖官方接口，当前允许暂缓；接口条件明确后再决定实现或登记延期。
9. `REDESIGN-00`：上述功能、交互、内容与延期边界全部确认后，才允许启动最终视觉重构。

依赖关系：

- `SCROLL-*` 已完成；后续列表、图片和横向切换必须继续复用现有单滚动链与刷新状态机，不得重新引入双重分页触发。
- `SWIPE-*` 依赖详情页滚动与图片灯箱手势稳定。
- `BATCH-*` 依赖 `DOWNLOAD-*` 的任务模型、持久化和失败恢复能力；实现时应先完成下载任务底座。
- `ICON-*` 是 `NAVGLASS-*` 与最终视觉重构的图标基础；面性状态不得通过重新混入其他图标集解决。
- `NAVGLASS-*` 应先作为独立功能完成性能和可访问性验证，最终重构阶段只允许在既有语义上继续精修。
- `REDESIGN-00` 是严格的最后阶段，不得与功能开发并行，也不得为了视觉效果改写、删除或弱化现有业务。

## 6. 最终阶段：全软件视觉重构

- [ ] `REDESIGN-00` 按 [`Final_Visual_Redesign.md`](Final_Visual_Redesign.md) 执行完整设计系统与全页面视觉重构。只有其启动门槛全部满足后才能开始；必须经过多轮页面巡检与双端验收，不得以换色、统一圆角或少量组件翻新代替。

阶段里程碑（用于追踪 `REDESIGN-00` 内部进度，不单独计入任务总数）：

- [x] `REDESIGN-A` 完成现状问题、组件分散度、页面截图矩阵和信息架构审计；比较至少两套完整方向并冻结唯一视觉方向。详见 [`Visual_Redesign_Audit.md`](docs/redesign/Visual_Redesign_Audit.md)。
- [ ] `REDESIGN-B` 实现颜色、字体、间距、栅格、圆角、边框、阴影、图标状态、密度和动效 Token；建立真实内容组件样板并通过深浅色、响应式和可访问性验证。
  - [x] `REDESIGN-B1` 建立应用层 ThemeExtension 与颜色、字体、间距、栅格、圆角、边框、阴影、图标状态、密度、动效 Token；实现可增长的 Section / Entry Item 样板并接入真实页面。
  - [ ] `REDESIGN-B2` 完成按钮、输入、标签、卡片、菜单、Panel、空态、错误和 Lottie 加载样板，以及繁中、英文、高对比度、减少动画与跨平台状态矩阵。
    - [x] `REDESIGN-B2A` 统一 Button、TextField、Tag 与 ChoiceGroup 的尺寸、语义色、状态、触控、长文案和响应式规则；接入四种登录流程和标签详情筛选 Panel，并完成深浅色、高对比度与减少动态验证。
    - [x] `REDESIGN-B2B` 重构现有 Lottie 资产与运行组件：统一虚拟画布、有效内容区、光学校准、留白、主题语义色、循环 / 单次播放及减少动态降级；原关键帧、缓动和动作含义不得丢失。首页主导航动画使用统一 22px 光学尺寸，未选中帧必须清晰，选中时只播放一次且不得延迟页面切换。
    - [ ] `REDESIGN-B2C` 完成卡片、菜单、通用 Panel、空态、错误和其余加载样板，并补齐繁中、英文、跨平台与完整状态矩阵。
- [ ] `REDESIGN-C` 完成首页、作品详情、标签详情、作者主页和设置页五类代表页面试点，并至少进行两轮一致性与内容沉浸自检。
- [ ] `REDESIGN-D` 按页面族迁移全部剩余页面，删除旧样式和重复组件；每一族独立完成测试、截图、双端运行与提交。
- [ ] `REDESIGN-E` 完成设计一致性、信息层级、可访问性、响应式、语言、主题、性能和逐像素细节的多轮收口，以及重构前后功能入口无遗漏对照。

`REDESIGN-A` 完成记录（2026-08-24）：以 `dev/redesign@c9d72e3` 为重构前代码基线，扫描 62 个 Screen 与 30 个业务 Widget，并量化业务层 281 处直接颜色表达、140 处圆角、329 处 EdgeInsets 和 63 处动画时长；公共 `awesome_chewie` 运行层同样登记组件与样式分散度。MuMu 现场采集首页、搜索、动态、我的的浅色基线及首页、我的的深色基线，并归档既有平板横竖屏、Windows、详情、标签、个人主页、设置、登录、评论 Panel 和视频截图，共 19 张。方向比较后冻结“静谧内容工作室”：浅色页面与主导航保持纯白、现有青绿作为克制强调、作品提供主要色彩，以三档内容密度、统一版心、内容驱动高度和有限 Token 阶梯建立自身辨识度；不采用暖纸主底、装饰性衬线或仿官方布局。信息与入口清单已经冻结，阶段 B 不得越过 Token 和组件样板直接逐页换肤。

`REDESIGN-B1` 完成记录（2026-08-24）：新增应用层 `LoftifyDesignThemeData`，由现有 `ChewieThemeColorData` 派生语义色并完整保留用户自定义强调色和字体；冻结排版角色、2～40 间距阶梯、Section 10px 顶距、6 / 10 / 14 / 20 / 24 圆角、0.6 / 1 / 2 边界、两级阴影、48px 最小点击区域、三档内容密度、90～280ms 动效以及 compact / medium / expanded / large 宽度分级。全局 ThemeData 同步收口 AppBar 无分割线、浅色纯白页面与导航、输入、按钮、Dialog、BottomSheet、菜单和 Chip 的基础语义；Hot Reload 期间旧主题树缺少扩展时会安全降级，不再触发断言。首批 `LoftifySection` / `LoftifyEntryItem` 支持内容驱动高度、展开收起、长标题与说明、Lottie 加载、选中、禁用、成功、警告、错误、鼠标 / 键盘 / 按压反馈，并已迁移“我的”内容中心、我的创作和设置总页，入口与业务跳转未删减。新增 8 项 Token / 栅格测试和 3 项组件测试；全量 216 项测试通过，本轮文件定向静态检查零问题，Android Debug APK 与 Windows Profile 构建成功（Windows Debug 输出被用户正在运行的 Loftify 进程占用，未强制结束该进程）。MuMu Android 12 已完成浅色 / 深色“我的”、设置及首页 / 搜索 / 动态巡检，280px / 2 倍英文长文案由组件测试覆盖；Hot Reload、Hot Restart 均成功且运行日志无断言、溢出或未处理异常。独立 Android 真机仍未连接；繁中、完整英文、高对比度、减少动画和其余组件状态矩阵留在 `REDESIGN-B2`，因此 `REDESIGN-B` 继续保持未完成。

`REDESIGN-B2A` 完成记录（2026-08-24）：新增同源 `LoftifyButton`、`LoftifyTextField`、`LoftifyTag` 与 `LoftifyChoiceGroup`，统一 48px 最小触控区、三档尺寸、主 / 次 / 柔和 / 幽灵 / 危险按钮、焦点 / 悬停 / 按下 / 选中 / 禁用 / 加载 / 成功 / 警告 / 错误状态，以及可增长的长文案和窄屏换行规则；加载继续使用自定义 Lottie，不回退为系统进度圈。四种登录流程复用新输入与按钮，原自动填充、密码显隐、验证码和提交逻辑保持不变；标签详情筛选 Panel 移除易漂移的独立选择控制器，改由业务参数单向驱动，短标签优先行内排列，长英文与大字体可滚动，短屏上固定重置 / 确认区仍完整可见。新增 9 项组件与真实 Panel 回归，既有登录测试同步升级；全量 225 项测试、目标静态检查、差异检查和 Android Debug APK 构建全部通过。MuMu Android 12 已完成真实标签筛选 Panel 的浅色、深色、选中态、滚动、Hot Reload 与 Hot Restart 验证，运行日志未发现断言、布局溢出或未处理异常；独立 Android 真机当前未连接。Lottie 资产归一化与导航动画进入 `REDESIGN-B2B`，卡片及其余状态矩阵保留在 `REDESIGN-B2C`，因此 `REDESIGN-B` 继续保持未完成。

`REDESIGN-B2B` 完成记录（2026-08-24）：全部 43 个 Lottie JSON 已登记原始画布与有效内容区，新增统一虚拟光学视口，调用方可像普通图标一样直接指定 20 / 22 / 24 / 32px，不再受原 JSON 画布比例、标签留白或大效果区影响；原始图层、关键帧、缓动、震动、循环与庆祝效果均未重写。加载动画由整体染色改为强调色弧线、次强调色弧线与中性底层的分层语义配色，浅色与深色下均不再出现整块绿色。首页四个主导航增加统一 48×48 源画布、22px 光学尺寸的 Lottie：未选中时停在清晰轮廓帧，点击后页面立即切换并仅播放一次，离开时复位；减少动态时直接落在目标帧。新增资源完整性、光学尺寸、分层配色、减少动态与导航单次播放回归；全量 229 项测试、目标静态检查与 Android Debug APK 构建通过。MuMu Android 12 已完成导航选中 / 复位、加载动画浅色 / 深色、Hot Restart 与 Hot Reload 验证；独立 Android 真机当前未连接。卡片、菜单、通用 Panel 和其余状态矩阵进入 `REDESIGN-B2C`，因此 `REDESIGN-B2` 与 `REDESIGN-B` 继续保持未完成。

`REDESIGN-R01`～`REDESIGN-R03` 完成记录（2026-08-24）：确认视觉回归来自全局 Material 排版映射与内容表面映射，而不是帖子详情业务组件本身。旧页面的 `titleLarge / titleMedium / titleSmall` 已恢复 18 / 16 / 14，`bodyLarge / bodyMedium` 恢复 16 / 14；新设计 Token 仍保留独立排版角色，只由明确迁移的新组件使用，避免旧页面已有 `fontSizeDelta` 被二次放大。浅色页面和导航继续保持白色，`canvasColor` 与 `cardColor` 则恢复用户所选主题的原始浅灰层级，帖子详情 AppBar 合集胶囊、正文底部合集 / 粮单 / 标签以及其他旧控件不再白底叠白底。28 项主题、背景、详情和组件测试通过；MuMu Android 12 已现场确认首页与“我的”字号，以及帖子详情顶部合集、底部合集导航和标签背景，并完成 Hot Reload / Hot Restart。运行日志未发现 Loftify 断言、溢出或未处理异常；独立 Android 真机当前未连接。

`REDESIGN-R10` 完成记录（2026-08-24）：公共图标按钮的默认点击形状已由矩形圆角收口为圆形，普通页面 AppBar 的返回、更多和下载等独立操作会统一复用该规则；只有显式传入圆角的行内工具按钮继续保留矩形布局。横屏响应式 AppBar 的返回入口同步改用圆形公共按钮。11 项图标组件测试通过，目标静态检查无问题；MuMu Android 12 已在帖子详情页完成 Hot Reload 与 Hot Restart，运行日志未发现框架异常。

`REDESIGN-R04`～`REDESIGN-R06` 完成记录（2026-08-24）：首页、搜索、关注和“我的”已移除完整横向 AppBar，统一接入 48px 全圆角悬浮胶囊、柔和边界与轻量阴影；支持毛玻璃和“降低透明效果”实色降级，并在高对比度、减少动画等辅助模式下自动停用模糊。首页头像可直达“我的”；搜索保留左侧头像，右侧搜索按钮会平滑展开为行内输入胶囊；关注四 Tab 使用主题色 `UnderlineIndicator`、无水波纹且平均铺满；“我的”左侧为通知，右侧仅保留主题和设置，装扮移入内容中心，下载管理、创作入口和退出登录均未删除。四页的安全区与首屏顶部留白现在属于各自滚动内容，首屏不被悬浮控件遮挡，上滑后内容可自然延伸进状态栏而悬浮控件保持原位。17 项悬浮层、底部导航与内容中心测试连同图标、Tab 专项共 33 项通过；目标静态检查无 error / warning。MuMu Android 12 已逐页完成浅色真实数据、搜索展开、关注 Tab、内容中心、首页沉浸滚动、Hot Reload 与 Hot Restart 验证，未发现溢出、断言或框架异常；截图保存在 `build/screenshots/loftify_search_capsules.png`、`build/screenshots/loftify_dynamic_floating_tabs.png`、`build/screenshots/loftify_mine_floating_header.png` 与 `build/screenshots/loftify_home_immersive_scroll.png`。

`REDESIGN-R07` 完成记录（2026-08-24）：首页、搜索、动态和“我的”四个 48×48 导航 Lottie 已从单一线稿缩放重构为三层状态动画：未选中稳定停在 Lucide 语义一致的线性轮廓，选中时线稿淡出、面性图形带克制回弹淡入，并用两个短促点缀强化完成感，结束后稳定停在面性帧；四个资源仍使用统一 22px 光学视口和当前主题色。减少动画模式会直接跳到结束帧，不播放中间过程；页面选择依旧即时生效，Lottie 只播放一次且离开后复位。新增资源结构回归，验证四个资源均为 48×48、约 0.8 秒、包含 Outline / Selected Fill / Selection Spark，且首尾透明度严格从线性切换为面性；18 项 Lottie 与底栏专项测试通过。MuMu Android 12 已完成四入口连续切换、结束帧、Hot Restart 与 5 秒实机录屏验证，过程未出现空白帧、颜色跳变或持续播放；录屏与抽帧保存在 `build/screenshots/loftify_nav_animation.mp4` 和 `build/screenshots/loftify_nav_animation_contact_sheet.png`。

`REDESIGN-R08` 完成记录（2026-08-24）：底部导航滚动显隐已改为单一连续进度驱动，透明度、向下位移、轻微缩放和布局高度使用同一帧同步变化；隐藏采用 260ms `easeInCubic`，恢复采用 `easeOutCubic`，不再由多个嵌套动画分别裁切。快速反向操作会从当前可见进度立即反向，不会先沿旧方向跳动；多滚动控制器只接受当前活动方向，列表回到顶部时导航始终恢复。减少动画或辅助导航模式下即时到达稳定状态，隐藏接近完成后同步停止点击响应。16 项专项测试覆盖真实长列表、快速反向、减少动态、60 / 90 / 120Hz 和稳定帧调度，目标静态检查无问题。MuMu Android 12 已完成首页瀑布流向上隐藏、向下恢复、Hot Reload 与 Hot Restart 验证，冷启动后未发现断言、布局异常或闪跳；截图保存在 `build/screenshots/loftify_r08_hidden.png` 与 `build/screenshots/loftify_r08_shown.png`。

`REDESIGN-R09` 完成记录（2026-08-24）：下拉 / 上拉 Lottie 的绘制尺寸现在受当前真实留白约束，拖拽初段从接近零尺寸自然放大，任何时刻都不会先于列表让位而越过可见间隙，因此不再出现下缘被列表裁切、像是控件盖住动画的观感。默认下拉动画收紧为 30px、触发 / 最大距离为 48 / 72px；上拉动画为 28px、触发 / 最大距离为 44 / 64px；嵌套列表同步使用 28px 与 44 / 64px，不再以过短触发距离破坏拖拽节奏。指示器保持在内容上层并显式裁切于自身视口，状态切换缩短为 180ms；上拉仍在距末尾 240px 时自动预取，无需拖到底部。9 项专项测试覆盖浅深色 Lottie、可用留白与绘制尺寸关系、程序化刷新、嵌套刷新、提前分页加载和空状态；目标静态检查无 error / warning。MuMu Android 12 已完成首页瀑布流慢速下拉、松手刷新、回弹、Hot Reload 与 Hot Restart，并用实机录屏逐帧确认从小到大、完整露出及收回过程；最终录屏和截图保存在 `build/screenshots/loftify_r09_final.mp4`、`build/screenshots/loftify_r09_final_pull.png` 与 `build/screenshots/loftify_r09_final_triggered.png`。

启动门槛：除明确依赖官方接口并已登记延期的任务外，所有功能、交互、内容和稳定性任务均已完成验收；主要页面信息与入口清单已冻结；已有行为测试和基线截图可用于证明重构前后没有功能遗漏。

验收：形成独立、统一、现代且适合 LOFTER 内容生态的第三方客户端视觉语言；内容始终优先于界面装饰，所有主要页面、组件状态、响应式尺寸、主题和可访问性均通过文档规定的多轮检查。

## 7. 每轮统一验收流程（重复执行，不计入任务总数）

- [ ] `QA-01` 对本轮涉及文件执行格式化和静态检查，不新增编译错误或警告。
- [ ] `QA-02` 为修复项补充最小回归测试，并运行相关测试集。
- [ ] `QA-03` 完成 Android Debug APK 构建。
- [ ] `QA-04` 在已连接的 Android 模拟器和真机启动验证；设备缺席时必须记录未覆盖项。
- [ ] `QA-05` 在调试会话中分别执行 Hot Reload 和 Hot Restart。
- [ ] `QA-06` 按需求覆盖浅色 / 深色、中文 / 英文、竖屏 / 横屏和关键边界状态。
- [ ] `QA-07` UI 专项保留验收截图；性能专项记录设备、刷新率、操作路径和复现结果。
- [ ] `QA-08` 对复杂布局和性能问题使用 Flutter Inspector / Performance Overlay / DevTools 定位并记录结论。

每轮交付记录至少包含：完成的任务编号、修改范围、测试结果、模拟器结果、真机结果、遗留风险和下一步任务。
