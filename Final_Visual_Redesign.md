# Loftify 最终视觉重构总纲

> 状态：锁定为整个项目的最后一项重点工作。
> 启动条件：功能、交互、内容、稳定性与可延期边界全部确认后方可开始。
> 关联任务：`Todo.md` 中的 `REDESIGN-00`。
> 适用范围：Android、Windows 及项目支持的其他响应式尺寸；浅色、深色与自定义主题。

## 1. 项目定位与重构目标

Loftify 是面向 LOFTER 内容生态与用户习惯的独立第三方客户端，核心场景包括作品浏览、图文阅读、标签探索、内容收藏、作者关注与社区互动。最终设计需要同时满足：

- 熟悉但不照搬：保留 LOFTER 用户对作品、标签、作者与互动关系的认知连续性，但不复制官方客户端，也不制造官方身份错觉。
- 内容优先：摄影、绘画、同人、文学、手作等作品是视觉主角，界面框架降低存在感，不与内容竞争注意力。
- 文艺但克制：体现创作社区的情绪价值与温度，避免矫饰、廉价渐变、高饱和大色块和无意义装饰。
- 精致且可持续：建立可复用、可测量、可扩展的设计系统，不再逐页凭感觉调整颜色、圆角和间距。
- 完整保留业务：不得遗漏功能、删减内容、改变业务含义或用装饰性入口代替真实能力。

最终结果应是一款成熟、现代、具有自身辨识度的第三方客户端：用户首先感受到作品与内容，其次才感受到界面的存在。

## 2. 严格启动门槛

以下条件未全部满足时，不得开始页面视觉改造：

- [ ] `Todo.md` 中所有不依赖官方接口的功能与体验任务已完成并验收。
- [ ] 依赖官方接口的划线评论、评论扩展等任务已明确实现条件，或由用户确认登记延期。
- [ ] P0、P1 无未关闭回归；主要页面不存在持续加载、崩溃、跳位、内容裁切或明显手势冲突。
- [ ] 首页、详情、灯箱、标签、搜索、个人主页、收藏、历史与设置的信息和入口清单已冻结。
- [ ] 关键流程已有自动化测试，重构前主要页面已有浅色 / 深色、手机 / 平板 / 桌面基线截图。
- [ ] Lucide 图标迁移与首页毛玻璃主导航已作为独立功能完成，不在最终重构中边设计边补功能。
- [ ] 当前工作区已提交且可构建，可随时对照或回退到重构前基线。

## 3. 不可违反的边界

- 不冒充 LOFTER 官方客户端，不复制官方商标性布局、专属视觉资产或容易造成官方误认的表达。
- 不删除、隐藏、合并或更改任何已有功能的业务含义；视觉分组变化必须保留可发现性。
- 不以“更简洁”为由移除信息，不以“更现代”为由增加没有真实功能的入口。
- 不把最终重构缩减为换主题色、调圆角、换字体、统一卡片或替换图标。
- 不在各页面直接硬编码一套相似但不同的样式；先建立语义 Token 和公共组件，再迁移页面。
- 不混用多套图标、阴影、圆角、间距或动效语言；受控例外必须登记原因、范围和退出条件。
- 不为了动效展示牺牲阅读、性能、可访问性或操作效率。

## 4. 重构前全局审计

开始设计前先产出审计文档和截图矩阵，至少覆盖：

### 4.1 字体与信息层级

- 字体家族、字号、字重、行高、字距和文字颜色的现有使用分布。
- 标题、正文、作者、元信息、标签、按钮、提示与错误文案之间的层级冲突。
- 中文、繁体中文和英文下的换行、截断、基线与可读性问题。
- 长文字正文宽度、段落间距和前景对比度是否适合持续阅读。

### 4.2 组件与布局

- 按钮、输入框、搜索框、标签、开关、卡片、菜单、导航、Tab、Panel、Dialog 与空状态的重复实现。
- 控件高度、点击区域、圆角、边框、内外间距、分割线和阴影的不一致项。
- 页面栅格、边距、内容最大宽度、图片比例、卡片节奏与视觉重心问题。
- 手机、横屏、平板与桌面之间突然跳变、内容挤压和操作错位问题。

### 4.3 图标与动效

- Lucide 之外的静态界面图标、旧位图和重复 SVG 清单。
- 图标视觉尺寸、线宽感、颜色、对齐和选中态表达差异。
- Lottie、页面过渡、加载、反馈和手势动画的节奏、时长与曲线差异。
- 无意义、冗长、遮挡阅读或导致持续重绘的动画。

### 4.4 内容呈现

- 作品卡片的封面比例、标题行数、作者信息、标签、发布时间、热度和互动按钮是否争夺注意力。
- 图片、插画、摄影、多图、长文、短文和视频在不同密度页面中的展示质量。
- 标签发现、筛选、选中、管理和失效状态是否清晰统一。
- 评论与社区互动的信息层级、阅读顺序和操作入口是否自然。

## 5. 设计系统交付物

设计系统必须先于全页面迁移完成，并以语义命名而非页面命名组织。

### 5.1 色彩体系

建立浅色、深色和自定义主题的完整语义色表：

- 页面底色、内容底色、悬浮层、遮罩、输入层、选中层和禁用层。
- 主文字、次文字、弱化文字、反色文字、链接与占位文字。
- 品牌强调、互动选中、成功、警告、错误和信息状态。
- 边框、分割线、焦点环、阴影与毛玻璃染色。

基础色应低噪声、中性、耐看并带适度社区温度；强调色只服务于关键操作和状态。必须验证正文、弱文字、按钮和图标的对比度，禁止依赖颜色作为唯一状态信号。

### 5.2 字体层级

制定显示标题、页面标题、区块标题、卡片标题、正文、长文正文、辅助信息、标签和按钮文字的：

- 字体家族与回退顺序。
- 字号、字重、行高、字距和最大行数。
- 中文 / 英文 / 数字混排规则。
- 响应式缩放边界与系统大字体策略。

长文需单独定义舒适正文宽度、段落间距、引用、链接、图片说明和列表样式，不能直接复用普通卡片正文。

### 5.3 间距、栅格与响应式

- 建立有限且明确的间距阶梯，覆盖图标间距、控件内边距、卡片间距、页面边距和区块节奏。
- 定义手机、宽手机、平板、横屏和桌面的断点、列数、沟槽、最大内容宽度和侧栏规则。
- 建立内容密度等级，让首页流、搜索列表、个人主页和设置页可以共享规则但保持合理密度差异。
- 所有布局必须支持安全区、系统手势区、键盘、长文本和动态字体。

### 5.4 圆角、边框与阴影

- 建立小控件、中型容器、卡片、Panel、Dialog 和全屏浮层的圆角阶梯。
- 区分结构边界、交互焦点和状态警告，避免所有区域都依赖描边。
- 阴影只表达真实层级，使用低透明、柔和扩散和有限层数；深色模式不能简单复用黑色阴影。
- 毛玻璃、透明层和纯色层必须有确定的层级及性能降级方案。

### 5.5 图标规范

- 默认静态界面图标统一使用 Lucide。
- 统一常用尺寸、视觉修正、颜色、点击区域、文字基线和状态透明度。
- 面性状态优先通过主题色、底板、容器形态、圆点或强调层表达，不重新混入 Material / Cupertino 面性图标。
- Lottie 动画保留；其静态占位、失败与无障碍替代仍遵循 Lucide 规范。
- 品牌标识、用户内容和受控配对 SVG 必须登记为例外，不得借例外扩散第二套图标语言。

### 5.6 动效规范

- 定义快速反馈、状态切换、Panel、页面过渡、内容进入和手势跟随的时长与曲线。
- 动效只解释空间关系、操作结果或状态变化；禁止循环装饰和冗长等待。
- 支持系统减少动画偏好；60/90/120Hz 下时长与速度一致。
- 模糊、阴影和大图变换必须验证重绘范围和帧时间。

## 6. 公共组件及完整状态

以下组件必须统一，并覆盖默认、悬停、按下、聚焦、选中、禁用、加载、成功、警告、错误和无内容等适用状态：

- 主按钮、次按钮、文字按钮、图标按钮、危险操作和组合操作。
- 输入框、搜索框、验证码、下拉菜单、行内选择与筛选器。
- 标签、筛选标签、状态徽标、角标和可管理标签。
- 开关、单选、复选、滑块、进度条和刷新控件。
- 内容卡片、作者卡片、列表项、设置项和管理项。
- AppBar、主导航、Tab、侧栏、悬浮操作条和面包屑。
- BottomSheet、Panel、Dialog、ContextMenu、Tooltip 与 Toast。
- 图片查看器、视频控件、评论区、加载、空状态、错误和重试。

每个公共组件需要记录：用途、禁止用途、尺寸、Token、状态、交互、语义标签、键盘行为、响应式变化和测试用例。

## 7. 内容类型设计原则

### 7.1 图片、插画与摄影

- 封面比例稳定，裁切策略明确，禁止压缩变形。
- 多图顺序、页码、预览与沉浸式灯箱关系清晰。
- 界面控件减少对画面的遮挡，必要操作可渐隐但必须可发现。

### 7.2 长短文字作品

- 长文优先保证阅读宽度、字号、行高、段落、引用与图片说明。
- 短文卡片只保留必要信息，避免标签、作者、数据和按钮同时高亮。
- 正文与互动区建立清晰结束边界，不让悬浮操作遮挡内容。

### 7.3 标签与兴趣发现

- 标签展示、点击、筛选、选中、屏蔽与管理使用同一语义体系。
- 热门、普通、已选、禁用和风险标签不能只靠颜色区分。
- 标签详情、搜索和首页入口保持认知连续性。

### 7.4 高频互动

点赞、推荐、收藏、评论、关注、分享和下载必须易识别，但只允许关键当前状态使用强调色；数字、状态变化、触觉与动画反馈保持一致，不制造彩色按钮墙。

## 8. 页面级迁移范围

按“基础 Token → 公共组件 → 代表页面 → 全量页面”的顺序迁移，不逐页另起炉灶。

1. 首页信息流与主导航：建立内容密度、卡片节奏、毛玻璃层级和选中状态基准。
2. 作品详情与长文阅读：验证内容优先、正文可读性、互动区和切篇体验。
3. 图片灯箱与视频：验证沉浸、手势、控件渐隐和横屏布局。
4. 标签页与搜索结果：验证筛选、Tab、列表密度和发现路径。
5. 作者主页：验证资料头、内容 Tab、关注与更多操作层级。
6. 收藏、喜欢、推荐、合集、粮单与历史：验证批量操作、状态和分组一致性。
7. 评论区与社区互动：验证作者、回复、时间、操作与多级内容的阅读顺序。
8. 设置、管理、登录、验证码和异常页：验证表单、列表项、状态反馈及品牌独立性。

每迁移一组页面，都必须与未迁移页面检查视觉断层；不得留下长期存在的半新半旧状态。

## 9. 实施阶段

### 阶段 A：审计与方向确认

- 产出现状问题清单、组件清单、页面截图矩阵和信息架构图。
- 提出至少两套完整视觉方向，比较内容沉浸、辨识度、实现成本和可访问性。
- 选定方向后冻结原则，不在编码过程中反复凭感觉改风格。

### 阶段 B：设计 Token 与组件样板

- 实现颜色、字体、间距、栅格、圆角、边框、阴影、图标和动效 Token。
- 用真实中文、英文、长标题、大图、长文和异常数据构建组件样板。
- 完成深浅色、响应式、键盘和无障碍状态验证。

### 阶段 C：代表页面试点

- 选择首页、作品详情、标签详情、作者主页和设置页作为密度不同的试点。
- 完整实现而非只做静态稿；保留真实数据、滚动、加载、失败、空态和操作反馈。
- 对照产品定位与设计系统至少进行两轮自检和调整。

### 阶段 D：全量迁移

- 按页面族批量迁移，删除旧样式和重复组件。
- 每完成一个页面族立即执行自动化、截图、双端运行与 Git 提交。
- 记录受控例外和未决项，禁止用临时硬编码赶进度。

### 阶段 E：多轮质量收口

- 第一轮：设计一致性、信息层级、内容沉浸和明显视觉断层。
- 第二轮：可读性、可访问性、状态反馈和功能可发现性。
- 第三轮：响应式、动态字体、中英文、深浅色和性能。
- 第四轮：逐像素细节，包括基线、图标对齐、点击区域、裁切、留白、列表节奏和视觉重心。
- 最后一轮：对照重构前功能与入口清单，证明无信息、功能、数据和交互遗漏。

## 10. 验收矩阵

### 10.1 必测页面

- 首页各 Tab、搜索与搜索结果、动态、我的。
- 图片、文章、视频、合集、粮单和标签详情。
- 作者主页、粉丝 / 关注、收藏、喜欢、推荐、历史。
- 评论 Panel、所有底部 Panel、更多菜单、分享、下载与管理页。
- 登录、验证码、锁屏、设置、主题、语言、字体与错误页。

### 10.2 必测状态

- 首次加载、刷新、分页、空数据、部分失败、整页失败、重试。
- 默认、悬停、按下、聚焦、选中、禁用、成功、警告和错误。
- 浅色、深色、自定义主题；简体、繁体、英文；系统字体与自定义字体。
- 小屏手机、宽手机、平板竖屏 / 横屏、桌面最小窗口与大窗口。
- 键盘、安全区、系统返回手势、减少动画、降低透明度和高对比度。
- 60/90/120Hz、长列表、大图、多图、长文和高密度评论。

### 10.3 完成证据

- 设计 Token 清单和组件目录。
- 重构前后主要页面截图矩阵。
- 自动化测试、静态检查和 Debug 构建结果。
- Android 模拟器、Android 真机与 Windows 的 Hot Reload / Hot Restart 记录。
- 主要流程功能对照清单与无遗漏结论。
- 仍保留的受控例外清单及理由。

## 11. 最终完成标准

只有同时满足以下条件，`REDESIGN-00` 才能勾选完成：

- 所有主要页面共享明确统一的视觉语言，不存在明显临时样式、旧组件残留或页面间断层。
- 内容层级、阅读体验、图片展示、标签发现与高频互动符合 LOFTER 内容生态，但保持第三方产品独立身份。
- 颜色、字体、间距、栅格、圆角、边框、阴影、图标和动效均由可持续设计系统驱动。
- 所有核心组件及适用状态完整，深浅色、语言、响应式和可访问性验证通过。
- 重构前已有信息、功能、数据和交互逻辑完整保留，测试与人工对照均未发现遗漏。
- 多轮自检中不再存在明显拥挤、零碎、突兀、廉价、抢夺内容注意力或与整体风格不一致的部分。

在达到这些标准之前，不得以“初版已完成”结束任务。
