# Loftify 视觉重构阶段 C：跨页面自检

> 上位规范：[`Final_Visual_Redesign.md`](../../Final_Visual_Redesign.md)
> 代表页面：首页、作品详情、标签详情、作者主页、设置页
> 完成条件：至少两轮真实数据、真实交互和跨尺寸检查；每轮发现的问题必须修复并重新验证。

## 第一轮：设计系统连续性与内容沉浸（2026-08-29）

本轮从“页面框架是否退后、作品是否仍是主角、相邻页面是否共享同一语义组件”出发，交叉检查五类代表页。检查同时覆盖代码实现、320px / 2 倍字体组件边界、浅深色 Token、既有真实页面行为和代表页回归测试。

| 页面 | 检查结论 | 本轮处理 |
| --- | --- | --- |
| 首页 | 页面使用纯白语义底色、响应式最大版心和真实列宽瀑布流；封面继续 `BoxFit.cover`，文字帖灰底只包正文。未发现新的内容竞争或栅格断层。 | 保留当前高密度内容流配方；用真实列宽和媒体裁切回归继续守卫。 |
| 作品详情 | 阅读版心和右侧悬浮操作条已进入新设计系统，但正文底部合集 / 粮单和 AppBar 合集入口仍使用旧 `cardColor`、局部圆角和 3px 间距，形成明显半新半旧断层；旧操作行在窄屏大字体下也缺少增长策略。 | 新增公共 `LoftifyContentReferenceCard` 与 `LoftifyContextPill`；统一语义色、细边界、14px 卡片圆角、48px 操作目标和无扩散水波纹反馈。320px 或大字体时订阅区和上下篇操作自动重排，正常宽度保持紧凑行内布局；首篇 / 末篇原提示及目录、订阅、切篇业务完整保留。 |
| 标签详情 | 标签头部、发现入口、筛选吸顶条和内容栅格均由 Token 驱动；原 Underline Tab、自动刷新和筛选记忆未被视觉层延迟。未发现新的视觉断层。 | 保留当前中密度发现配方；继续以长文案、自动刷新和真实单元格宽度测试守卫。 |
| 作者主页 | 身份、统计、主操作、代表作和正文圆角已形成稳定信息层级；主题头图上的白色与半透明黑仅用于保证媒体覆盖层对比，登记为受控内容叠层，而非第二套页面色彩。 | 保留当前主题媒体叠层；继续以 320px / 2 倍字体、宽屏双栏和 Tab 刷新回归守卫。 |
| 设置页 | Caption / Entry / 行内选择和 AppBar 已统一，但“发现新版本”说明仍直接使用 `Colors.redAccent`，会绕过自定义主题与独立状态色。 | 改为 `design.colors.warning`，继续使用同一危险 / 警告 / 成功语义体系；页面 10px 外边距、Caption 10px 顶距和无 AppBar 分割线规则保持不变。 |

### 第一轮交叉结论

- 浅色主页面继续以纯白承载内容；灰色只用于明确的低强调表面，不新增整块灰色评论或卡片墙。
- 普通 AppBar 独立操作继续使用圆形点击面；只有带文字的内容关系入口使用紧凑胶囊，未把返回 / 更多按钮改成矩形圆角。
- 首页高密度、详情与主页中密度、设置低密度的差异继续由同一 Token 体系表达，没有逐页复制常量。
- 真机快速反向拖动“我的”列表时额外发现 EasyRefresh 的旧惯性仍在变化后的内容边界外运行，返回的 overscroll 大于当前帧位移。公共滚动物理现在会完整放行向合法区间的回弹，并把向外限制约束在当前帧位移内；底栏显隐改变视窗高度时不再抛出未处理异常。
- 本轮发现的三个真实断层均已修复并重新验证，第一轮通过。

### 第一轮完成证据

- 45 项代表页与滚动专项测试通过，覆盖设计 Token、首页真实列宽、作品详情、标签头部、作者头部、设置节奏、320px / 2 倍字体、浅深色和内容范围缩短时的弹道回弹。
- 涉及文件定向静态检查为 0 error / 0 warning；46 项 info 均为作品详情既有类型推断与弃用提示，本轮没有新增。
- Android Debug APK 与 Windows Profile 构建通过。
- Android 16 真机 `986008e8`（1200×2670 / 120Hz）以真实登录数据逐页进入首页、作品详情、标签详情、作者主页、设置和通用设置；作品详情 AppBar 合集胶囊实测 36dp 高，下载 / 更多保持圆形，正文合集操作实测 48dp 高，真实合集目录 Panel 可打开并通过系统返回关闭。
- 修复后在真机连续执行四轮快速反向拖动与底栏显隐，运行日志为 0 个滚动物理、断言、RenderFlex 溢出或未处理异常；Hot Reload 616ms、Hot Restart 2282ms。
- 真机当前开启安全模式，ADB 截图会被系统安全层遮蔽；本轮保留可访问性节点、物理边界和运行日志作为实机证据，第二轮仍须补充当前构建的可视截图，不能用旧截图代替最终验收。

## 第二轮：可读性、状态和极端尺寸

进行中。本轮在第一轮最终构建基础上重新检查，不复用第一轮结论；按用户最新要求停止桌面端验收，当前只继续 Android 手机与平板、深色与自定义主题、简中 / 繁中 / 英文、大字体、空态 / 错误 / 加载和系统返回手势。完成剩余矩阵并补齐当前构建截图前，不勾选 `REDESIGN-C6`。

### P2A：紧凑屏与大字体 AppBar（2026-08-29）

- 以 320dp、2 倍字体和英文长文案组合真实还原作品详情 AppBar 的返回、标题、合集胶囊、下载与更多入口，首次运行复现 `RenderFlex overflowed by 1321 pixels`。
- `LoftifyContextPill` 不再在所有设备固定使用 180dp 最大宽度：小于等于 360dp 收敛到 96dp，360～420dp 根据文字缩放使用 104 / 128dp，更宽设备保持 180dp。视觉标签安全省略，36dp 最小高度、图标、点击行为及完整语义标签保持不变。
- 公共 `ResponsiveAppBar` 在横向布局中把标题限制到剩余空间，并为内建标题统一单行省略；帖子详情自定义标题同步处理。Android 平板横屏或长本地化标题不再把尾部操作推离屏幕。
- 23 项 AppBar、浅深色内容归属卡及帖子详情组件回归通过；定向静态检查 0 error / 0 warning，46 项 info 均为帖子详情既有问题。
- 本小项已完成并可独立提交；第二轮的主题、三语言、状态、系统返回与当前构建截图仍待继续。

### P2B1：短屏状态反馈与重试可达性（2026-08-29）

- 以 320×220dp、2 倍字体和长英文标题 / 说明 / 重试文案检查错误状态，首次运行复现底部 `RenderFlex overflowed by 788 pixels`，重试操作无法到达。
- 页面级加载、错误、警告和成功状态在高度受限时改用 Android Clamping 滚动；内容未超高时仍保持原居中布局，不缩放文字或删除说明。
- EasyRefresh 与 `SliverFillRemaining` 空态继续由原外层滚动承载，内部不创建 `LayoutBuilder` 或第二个滚动区；通知页占位、下拉刷新与触底预加载手势保持不变。
- 29 项状态表面、通知空态、Lottie 刷新、提前分页、深色语义状态与帖子局部解析错误回归通过；涉及文件静态检查无问题。
- 本小项已完成并可独立提交；第二轮仍需继续主题、三语言、真实状态切换、系统返回与当前构建截图。

### P2B2A：纯图标加载态语义（2026-08-29）

- 纯图标加载场景会传入已本地化标题但设置 `showText: false`；此前该标题也从语义标签移除，而 Lottie 动画本身又被排除语义，屏幕阅读器只能获得无名称的 live region。
- 状态容器现在始终保留非空标题和说明的组合语义；视觉标题是否显示不再影响辅助技术。可见标题 / 说明不再各自重复生成语义，重试按钮仍作为独立按钮节点。
- 纯图标加载专项由失败转为通过，状态表面、通知 Sliver 空态和 Lottie 共 19 项回归通过。
- 本小项已完成并可独立提交；主题、三语言、真实状态切换、系统返回与当前构建截图仍待继续。

### P2B2B1：当前真机构建的安全截图通道（2026-08-29）

- Android 16 真机启用系统安全层后，普通 ADB 截图会被遮蔽；验收不关闭用户防截屏、不修改系统安全设置，也不使用旧版本图片代替当前构建。
- 新增仅用于开发验收的 Flutter 渲染截图工具，通过调试服务请求当前根渲染树截图；画面直接来自真机仍在运行的 Debug 构建，不包含或读取登录凭据，也不改变应用运行逻辑。
- 已在真机当前进程成功生成 `build/screenshots/loftify_c6_p2_current_zh_light.png`，确认浅色简中真实关注流、封面裁切、顶部导航和底部导航无明显裁切、变形或布局断层。
- 工具地址转换专项测试与静态检查通过；该通道只补足应用渲染层证据，系统状态栏、安全遮罩和系统返回仍须通过真机交互单独验收。
- 本小项已完成并可独立提交；深色、自定义主题、繁中、英文、大字体、真实状态切换和系统返回继续留在 `REDESIGN-C6-P2B2B`。

### P2B2B2：主题强调色前景对比（2026-08-29）

- 第二轮主题矩阵首先以默认极简白的青绿色强调色复现问题：旧黑白前景算法在相对亮度不超过 0.5 时一律选择白色，实际白字与默认青绿色仅有约 2.22:1 的对比度；橙色、绿色等较亮自定义主题同样不可读。
- 基础颜色逻辑改为分别计算黑色和白色前景的 WCAG 对比度并选择更高者；Material `onPrimary`、新设计系统 `onAccent`、Chewie 旧组件及主题色板选中对号共用同一结果，避免新旧组件继续出现两套前景色。
- 新增内置全部浅色 / 深色主题以及青绿、橙色、绿色自定义强调色回归，所有强调色前景均达到至少 4.5:1；主题刷新、背景和三语言基础回归共 22 项通过，定向静态检查无问题。
- Android 16 真机 `986008e8` 已完成完整 Debug 编译、覆盖安装、Hot Reload 685ms 与 Hot Restart 2303ms；之后 USB 断开，但当前修复已安装到真机。桌面端未执行。
- 本小项已完成并可独立提交；三语言页面视觉、大字体、真实状态切换和系统返回继续留在 `REDESIGN-C6-P2B2B`。

### P2B2B3：持久化语言的冷启动同步（2026-08-29）

- 明确选择繁中或英文后，Flutter 本地化会从持久化值恢复，但旧启动刷新逻辑只在“跟随系统”时设置 `Intl.defaultLocale`；冷启动可能出现界面文案已切换、日期等格式化仍沿用进程默认语言的混合状态。
- 启动和系统语言变更时现在始终把 `intl` 同步到“用户明确选择的语言，或解析后的系统语言”；只有跟随系统时才因系统语言变更通知整棵界面刷新，明确语言不会被系统变化覆盖。
- 冷启动繁中失败用例已转为通过；简中 / 繁中 / 英文解析、系统语言刷新、年月格式、本地化资源和窄屏大字体设置布局共 12 项回归通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机在上一小项后 USB 断开，本小项 APK 尚待设备恢复连接后覆盖安装；因此三语言真机页面视觉仍保留在 `REDESIGN-C6-P2B2B`，不能仅凭自动化勾选第二轮完成。桌面端未执行。

### P2B2B4：主导航系统返回分流（2026-08-29）

- 主导航外层 `PopScope` 旧回调忽略 `didPop`：即使根路由已经成功返回，仍会再次执行内部 Panel 的返回清理，离场过程中可能重置当前页、底栏与 Panel 显示状态。
- 新增独立返回作用域，只在根路由被阻止时把 Android 系统返回委托给内部 Panel；根路由已成功返回时不再执行第二次清理。原内部页面关闭、根页面退出和视频左滑作者页返回语义保持不变。
- 组件测试分别验证“根返回关闭时只处理内部页”和“根路由已返回时不调用内部清理”，并复测视频作者页系统返回后视频仍保持在原位置；3 项通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待恢复连接后覆盖安装并执行真实返回手势；因此系统返回真机验收仍保留在 `REDESIGN-C6-P2B2B`。桌面端未执行。

### P2B2B5：主题柔和选中态对比（2026-08-29）

- 主强调按钮对比修复后继续检查 `accentContainer / onAccentContainer`，默认极简白青绿主题仍复现约 4.12:1 的不足；标签筛选、合集胶囊、选中菜单、卡片和 tonal 按钮共用该颜色对，不能只修单个页面。
- 柔和选中态现在先保留满足 4.5:1 的主题色前景；主题色及其原候选都不足时，回退到同一主题的语义正文色。选中底板仍保留低饱和强调色，业务状态、圆角和信息密度不变。
- 全部内置浅色 / 深色主题及青绿、橙色、绿色自定义主题的柔和选中态均达到至少 4.5:1；主题、按钮、卡片、合集关系卡和标签详情共 34 项回归通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待恢复连接后覆盖安装并检查真实选中态；阶段 C 第二轮继续未完成。桌面端未执行。

### P2B2B6：成功 / 警告 / 危险状态可读性（2026-08-29）

- 状态矩阵继续复现默认浅色成功绿在白底仅约 2.78:1，警告黄更低；这些颜色被状态页、设置提示、菜单、卡片、输入校验和下载状态直接作为文字或图标前景使用。
- 三类状态色现在保持原主题色相，并只向当前明暗模式的高对比方向做满足 4.5:1 所需的最小调整；同时检查纯页面、抬升表面以及 10% 同色柔和底板，避免只在纯白背景达标。
- 全部内置浅色 / 深色主题的成功、警告、危险色在四类实际背景上均达到至少 4.5:1；主题、按钮、卡片、Entry、状态页、合集关系卡、设置与下载状态共 48 项回归通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并检查真实状态页与设置提示；阶段 C 第二轮继续未完成。桌面端未执行。

### P2B2B7：低强调文字可读性（2026-08-29）

- 文字层级矩阵此前只约束主文字和次文字；输入提示、空态辅助信息等使用的浅色 `textMuted` 在白底仅约 3.01:1，低于普通文字 4.5:1。
- `textMuted` 现在按页面与柔和表面分别校验，并向当前明暗模式的高对比方向做最小调整；主文字、次文字和低强调文字的相对层级保持不变，没有放大字号或改成高饱和颜色。
- 内置浅色 / 深色主题的三档文字在页面与柔和表面均达到至少 4.5:1；主题、输入、状态、Entry、设置和导航头共 38 项回归通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并检查真实输入提示与空态辅助信息；阶段 C 第二轮继续未完成。桌面端未执行。

### P2B2B8：Android 异步状态恢复链路（2026-08-29）

- 新增 Android 320×568dp、1.6 倍字体下的确定性异步状态回归，按实际交互顺序覆盖初始 Lottie 加载、网络错误、点击重试、重新加载和成功反馈。
- 每个阶段均校验状态图形、按钮与 live-region 语义同步更新；统一状态过渡结束后旧错误图标和重试入口彻底移除，不会与新加载或成功反馈叠加。
- 回归确认加载阶段始终使用应用自定义 Lottie，不会回退到系统 `CircularProgressIndicator`；状态表面专项 10 项通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并触发真实网络状态切换；三语言、深色 / 自定义主题、系统返回与当前构建最终截图继续保留在 `REDESIGN-C6-P2B2B`。按用户要求只处理 Android，桌面端未执行。

### P2B2B9：状态离场期间的重复操作防护（2026-08-29）

- 异步恢复回归进一步发现：点击重试后旧错误状态会按设计淡出，但淡出组件仍短暂留在命中树，Android 快速连点可能重复调用重试回调。
- `LoftifyStateView` 保留原状态淡入淡出和布局关系，只将正在离场的旧状态立即设为不可命中；当前新状态继续正常响应，不延迟加载状态或业务请求。
- 专项测试在首次点击已经进入 Lottie 加载后立即再次触碰旧按钮位置，重试回调始终只执行一次，随后完成态和旧控件清理均正常；状态表面 10 项通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装后补充真实高频触控验收；按用户要求只处理 Android，桌面端未执行。

### P2B2B10：繁中页面状态文案一致性（2026-08-29）

- 三语言资源复查发现通知空态的“暂无通知”和帖子详情首次加载的“文章加载中...”仍为简体字，会直接破坏繁中页面状态的一致性。
- 两处分别修正为“暫無通知”和“文章載入中...”，保留原业务含义、标点和加载流程，并与已有“重試”文案保持同一语言环境。
- 新增资源级测试直接验证生成后的繁中本地化类，同时继续校验全部语言键和占位符一致；本地化与状态表面共 14 项通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并检查真实通知空态与文章加载首帧；繁中整页视觉矩阵仍留在 `REDESIGN-C6-P2B2B`。按用户要求只处理 Android，桌面端未执行。

### P2B2B11：弱化表面的低强调文字对比（2026-08-29）

- 文字对比矩阵此前只覆盖页面与普通表面；扩展到浮起表面和弱化表面后，浅色 `textMuted / surfaceMuted` 为约 4.377:1，深色为约 4.388:1，未达到普通文字 4.5:1。
- 低强调文字现在以页面、普通、浮起和弱化四类真实底板共同计算最小可读颜色，只向当前明暗模式的高对比方向做必要调整；主 / 次 / 低强调层级、字号和低饱和视觉关系保持不变。
- 测试一次收集并报告全部失败组合，避免只修首个断言；主题、按钮、卡片、状态与 Entry 共 32 项回归通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并检查真实输入提示、禁用项和柔和卡片；按用户要求只处理 Android，桌面端未执行。

### P2B2B12：品牌强调底色与可读强调前景分层（2026-08-29）

- 既有矩阵只验证“强调色底板上的文字”；默认亮青绿直接作为白底文字、图标、焦点环和进度条时仅约 2.2:1，选中状态能识别但不够清晰。
- 新增 `accentForeground` 语义角色：原始 `accent` 继续完整承载用户主题色，用于主按钮填充、Lottie、选中底板和低透明交互染色；前景角色保持同一色相，只做满足四类中性底板 4.5:1 的最小明度调整。
- Material 文字 / 描边按钮、输入焦点和底栏选中态，以及 Tag、Entry、菜单、状态卡、装扮入口、帖子合集订阅和下载进度已统一接入可读前景；作者页固定白色强调操作面按实际合成白底独立求同色相可读前景，浅深主题均达到 4.5:1。全部内置浅深主题及亮青绿 / 橙 / 绿自定义主题均达标；主题与六类代表组件共 49 项回归通过，定向静态检查无新增 error / warning（详情页仅保留既有 info），Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并对比真实主题色前景；按用户要求只处理 Android，桌面端未执行。

### P2B2B13：装饰边界与可操作强边界分层（2026-08-29）

- 边界矩阵发现原 `outlineStrong` 在浅色白底、柔和底板和深色表面均不足 3:1；次按钮、输入悬停、可操作把手与高对比状态会因此显得边界缺失。
- `outline` 继续保留为内容卡片、区块和普通分割线的低噪声装饰边界；只有 `outlineStrong` 按页面、普通、浮起和弱化四类表面计算达到 3:1 所需的最小明度调整，避免全局边框变脏变重。
- 全部内置浅深主题的可操作强边界均达标，次按钮实组件确认读取新 Token；主题、控件、表面与导航头共 36 项回归通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并比较真实输入、次按钮和 Panel 把手；按用户要求只处理 Android，桌面端未执行。

### P2B2B14：作者页危险操作的稳定表面（2026-08-29）

- 作者页拉黑状态操作原先使用 20% 透明危险色底板与白字，叠加浅色页面或不同明暗的作者头图时无法保证文字对比，危险状态边界也不稳定。
- 拉黑状态现在使用与关注主操作一致的 94% 白色稳定表面；危险前景基于实际合成白底计算同色相的最小可读调整，并增加危险状态边框，不依赖背景图片提供对比。
- 浅色与深色主题均验证危险边框、至少 4.5:1 的文字对比和点击回调；个人主页组件相关 6 项回归通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并在真实作者头图上检查拉黑状态；按用户要求只处理 Android，桌面端未执行。

### P2B2B15：作者头图上的固定可读遮罩（2026-08-29）

- 作者页头图原遮罩从 18% 黑色开始，接近白色的封面会让白色 AppBar、昵称、ID 与元数据直接失去对比；实际可读性依赖用户上传内容，不受主题系统控制。
- 新增同源 `LoftifyProfileCoverScrim`，以 58%～72% 连续暗色渐变覆盖头图；ID 与元数据的白色不透明度统一为 88%，仍保留主次层级但不再过淡。
- 以纯白封面作为最坏输入，最浅渐变位置的主信息和辅助信息均达到至少 4.5:1；个人主页组件相关 7 项回归通过，定向静态检查无问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并比较真实明亮 / 深色作者封面；按用户要求只处理 Android，桌面端未执行。

### P2B2B16：作品集合头图共用可读遮罩（2026-08-29）

- 合集和粮单详情此前只模糊用户封面，没有暗色遮罩；白色 AppBar、标题、作者信息和统计依赖图片自身明暗，统计辅助标签还只有 60% 白色。
- 作者页遮罩提升为公共 `LoftifyCoverScrim`，作者、合集和粮单三类头图统一使用 58%～72% 渐变；合集 / 粮单统计辅助前景统一为 88% 白色，原封面 `BoxFit.cover`、模糊与内容结构保持不变。
- 纯白封面下主 / 辅助前景均达到至少 4.5:1，源文件守卫确保三类页面持续接入公共组件；相关 9 项回归通过，定向静态检查无新增 error / warning（合集与粮单保留 16 条既有 info），Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并比较真实合集 / 粮单封面；按用户要求只处理 Android，桌面端未执行。

### P2B2B17：图片角标的最低可读遮罩（2026-08-29）

- 23 处图片计数、类型、热度和灯箱角标共用 `buildTranslucentTag`，默认 40% 黑底，部分页面显式传入 20%；白字叠加纯白图片时最低只有约 2.8:1。
- 公共媒体覆盖规范增加 58% 最低角标遮罩；较浅的调用参数自动收敛到可读下限，更深的自定义遮罩不变，尺寸、图标、圆角和文案均保持原逻辑。
- 专项回归以 20% 请求和纯白图片验证实际合成结果达到至少 4.5:1；媒体覆盖相关 3 项通过，定向静态检查无新增 error / warning（`item_builder` 保留 3 条既有 info），Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并比较标签详情 Banner、装扮列表和图片灯箱角标；按用户要求只处理 Android，桌面端未执行。

### P2B2B18：壁纸预览顶部文字保护区（2026-08-29）

- 装扮壁纸卡片的白色日期与时间原本直接叠在用户图片上，浅色壁纸无法阅读；整图压暗则会破坏壁纸预览的真实性。
- 新增公共 `LoftifyTopTextScrim`，顶部以 66%～58% 暗色保护文字，向下完全透明；遮罩在圆角内裁切并由 `IgnorePointer` 排除命中，不影响预览点击和 Hero 灯箱。
- 纯白壁纸下保护区白字达到至少 4.5:1，底部透明与真实页面接入均有回归守卫；媒体覆盖相关 5 项通过，定向静态检查无新增 error / warning（装扮列表保留 11 条既有 info），Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并比较明亮 / 深色壁纸卡片；按用户要求只处理 Android，桌面端未执行。

### P2B2B19：装扮详情 Panel 的短屏响应式（2026-08-29）

- 装扮详情原自建容器包含固定 300px 预览、不可滚动正文和固定横排操作；320×568、2 倍字体回归复现底部溢出 57px，下载与确认入口会离开视窗。
- 详情迁移到统一 `LoftifyPanel`，复用公共顶部圆角、把手、表面和分割线；正文维持单滚动链，短屏 / 大字体将操作区纳入滚动，正常高度仍固定底栏。
- 单图、全部和确认统一使用 `LoftifyButton`，手机或大字体纵排、560dp 以上正常字号横排；下载异常会在 `finally` 中关闭加载层。2 项真实布局与源码守卫通过，定向静态检查零问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并验证真实装扮详情滑动、下载与安全区；按用户要求只处理 Android，桌面端未执行。

### P2B2B20：装扮下载的真实线性进度（2026-08-29）

- 装扮详情单图 / 全部下载仍弹出通用 Lottie 加载对话框，无法表达实际传输进度，也会遮蔽整个 Panel。
- 新增 `LoftifyDownloadProgressButton`，在统一按钮底边显示真实确定性线性进度与百分比，并向辅助功能暴露实时 value；下载中阻止当前 / 另一下载入口重复入队，同时禁用确认关闭。
- 图片保存底座补齐单图字节回调和多 URL 批量入口；批量下载复用等权聚合器，未知总字节保留份额，成功短暂停留到 100%，减少动态模式立即恢复。7 项真实进度、窄屏大字体、Panel 与多图聚合回归通过，6 个目标文件静态检查零问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并执行真实单图 / 多图下载进度验收；按用户要求只处理 Android，桌面端未执行。

### P2B2B21：表情包与装扮部件下载进度统一（2026-08-29）

- 表情包详情与装扮部件详情仍使用阻断页面的通用 Lottie 加载对话框，既看不到真实传输进度，也无法区分多个独立资源的下载状态。
- 表情包卡片改用带百分比的线性进度按钮；空间紧凑的装扮部件卡片改用圆形确定性进度环。两者都按资源 URL 独立记录状态，仅阻止同一资源重复点击，不影响浏览或下载其他资源。
- 两类入口统一复用图片保存底座的真实字节回调，成功后短暂停留在 100%，异常与组件销毁时安全清理状态；同时修正表情包首次刷新未返回 `IndicatorResult` 的既有问题，避免刷新控件无法正确结束。
- 真实进度、窄屏大字体、圆形按钮语义、重复点击防护和源码守卫共 6 项通过，4 个目标文件定向静态检查零问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并执行真实表情包 / 装扮部件下载验收；按用户要求只处理 Android，桌面端未执行。

### P2B2B22：登录页面族的短屏与键盘可达性（2026-08-29）

- 短屏与大字体复查发现，验证码、手机号密码、LOFTER ID 和邮箱四种登录页都将“其他登录方式”用 `Stack + Positioned(bottom: 50)` 覆盖在表单上；Android 横屏、分屏或键盘弹出后会遮住输入框和登录按钮，而且底部入口无法随页面滚动。
- 新增公共 `LoftifyLoginLayout`，四种登录方式统一进入同一条 Clamping 滚动链。正常高屏仍用弹性留白保持底部视觉重心，短屏或 1.35 倍以上字体自动收紧节奏；表单限制为 520px、主操作限制为 360px，避免宽屏输入区失控拉长。
- “其他登录方式”长文案可换行，两侧分割线只占剩余空间；入口改为统一圆形描边按钮，并补齐 Tooltip、按钮语义、48px 点击目标和 Lucide 图标。图片验证码恢复 `BoxFit.cover`，原焦点顺序、自动填充、密码显隐、倒计时和登录跳转保持不变。
- 320×360、2 倍字体下输入、主操作和登录方式均可滚动到达且无溢出，1000px 宽度下表单不会超过 520px；布局、输入附件和源码守卫共 5 项通过，8 个目标文件定向静态检查零问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并验证真实软键盘、验证码刷新与系统返回；阶段 D 仍等待阶段 C 当前构建截图门槛，不因本次公共布局收口提前标记启动。按用户要求只处理 Android，桌面端未执行。

### P2B2B23：评论区统一 Panel 与可变视窗（2026-08-29）

- 评论正文与二级回复已经保持无灰色卡片背景、昵称垂直居中和回复目标下置，但外层评论区仍自建 16px 顶部圆角容器，并把“最新评论”作为列表内固定 50px 的吸顶项；它与其他 Panel 的圆角、把手、表面和标题层级不一致。
- 评论区迁移到公共 `LoftifyPanel`，统一 20px 顶部圆角、拖拽把手、柔和边界、抬升表面与标题排版；“最新评论”移到稳定 Panel 头部，列表只保留加载、错误、空态、一级评论和二级回复这一条滚动链，不再复制列表吸顶标题。
- 新增 `LoftifyCommentPanel` 作为可验证外框：高度取当前可见视窗的 82% 并限制为 760px，键盘占高时按 `viewInsets` 同步收缩；原 EasyRefresh 下拉、提前分页、Lottie 状态、评论点赞、作者跳转和二级回复加载逻辑保持不变，未新增依赖官方接口的评论能力。
- 320×300、2 倍字体下长标题无溢出且末条评论可滚动到达，360×640 加 280px 键盘占高时 Panel 高度正确收缩；评论 Panel、长评论、嵌套回复与详情动作共 14 项通过，本次目标文件静态检查零问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并检查真实评论刷新、分页、二级回复和系统返回；按用户要求只处理 Android，桌面端未执行。

### P2B2B24：帖子收藏夹选择 Panel 的紧凑布局（2026-08-29）

- 帖子收藏夹选择仍使用自建 20px 圆角容器、手工分割线和旧圆角按钮；固定 80% 高度下，标题右侧“新建”与底部取消 / 确认在 320px、2 倍字体或键盘占高时会共同挤压文件夹列表。
- 选择器迁移到公共 `LoftifyPanel`，统一顶部把手、圆角、表面、边界、标题与 footer；创建、取消和确认均改用 `LoftifyButton`，选择列表继续保留 EasyRefresh、提前分页、创建收藏夹和原选择结果回调。
- 正常设备保持 80% 可见高度并将“新建”放在标题行；小于 380px、1.35 倍以上字体或短视窗改用 92% 可见高度，把“新建”移到标题下方且底部操作竖排、收紧 padding。文件夹封面维持 `BoxFit.cover`，名称允许两行并安全省略，ID 与篇数使用统一元数据层级。
- 320×480、2 倍字体下新建和确认均可操作且无溢出，360×700 加 300px 键盘占高时 Panel 高度正确收缩；3 项布局与源码守卫通过，本次目标文件静态检查零问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并验证真实文件夹创建、刷新、选择与确认；按用户要求只处理 Android，桌面端未执行。

### P2B2B25：合集目录统一 Panel 与真实栅格宽度（2026-08-29）

- 合集目录仍使用自建 20px 圆角容器和固定 80% 高度；目录网格通过整机屏幕宽度猜测 `wh`，而 `GridView.extent(maxCrossAxisExtent: 160)` 在 320px Panel 内实际只分两列，传给帖子封面的尺寸又按三列计算，造成封面留白、缩放与实际单元格不一致。
- 外框迁移到公共 `LoftifyPanel`，正常设备使用 80% 可见高度，短屏、大字体和窄屏使用 92%，并限制最大 760px；合集封面继续 `BoxFit.cover`，标题和说明在紧凑环境允许两行，订阅操作改用统一 tonal / secondary 按钮并在小屏移到摘要下方。
- 新增 `LoftifyCollectionGridGeometry`：手机稳定使用三列，600px 以上按约 160px 密度扩展到最多八列；网格使用 `LayoutBuilder` 的真实可用宽度计算方格边长，帖子卡片收到的 `wh` 与渲染单元格完全一致。原下拉刷新、上下分页、正倒序、订阅与合集详情跳转保持不变。
- 320×420、2 倍字体下 Panel 保持单滚动视窗且末项可达，296px 内容宽度得到三个 94.67px 方格，1000px 得到六列；3 项布局、几何与源码守卫通过，本次目标文件静态检查零问题，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并检查真实目录刷新、正倒序、订阅、封面裁切与详情跳转；滑块验证码明确保留独立居中验证器形态，不机械迁移为底部内容 Panel。按用户要求只处理 Android，桌面端未执行。

### P2B2B26：作品归档页族的真实单元格宽度（2026-08-29）

- 从合集目录继续扫描发现，我的作品、我的推荐、合集详情和粮单详情仍使用 `GridView.extent(maxCrossAxisExtent: 160)`，同时固定向封面组件传入 `wh: 160`；手机实际格宽只有约 95～145px，图片解码、裁切、角标和点击区域均与渲染单元格不一致。
- 新增公共 `LoftifyPostArchiveGrid` 与 `LoftifyArchiveGridGeometry`，将内边距从约束中扣除后再计算真实方格边长，并把该值直接传给帖子封面；合集目录、我的作品、我的推荐、合集详情和粮单详情五个入口全部迁移，移除各页重复的固定 160px 实现。
- 手机继续保持用户熟悉的三列归档，600px 以上按约 160px 目标密度增加到最多八列，封面不会因窗口变宽而无限拉伸；外层页面仍持有唯一滚动链，月份分组、位置恢复、刷新、分页、批量下载和帖子跳转逻辑不变。
- 320px 宽并含 12px 两侧 padding 时，组件实测生成三个 94.67px 正方形且回调尺寸一致；296px / 1000px 几何、五入口源码守卫与合集 Panel 共 5 项通过，公共组件与测试静态检查零问题，旧页面无新增 error / warning，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并比较五个真实页面的封面裁切、角标、月份分组和滚动位置；按用户要求只处理 Android，桌面端未执行。

### P2B2B27：收藏与历史归档网格收口（2026-08-29）

- 继续扫描后发现，收藏夹详情、我的喜欢和浏览历史仍以 `maxCrossAxisExtent: 160` 布局，却固定向封面传入 `wh: 160`；手机三列实际格宽远小于 160px，同样会造成解码尺寸、裁切和角标与渲染区域不一致。
- 公共归档组件新增 `LoftifyPostArchiveSliverGrid`，直接使用 Sliver 的真实横轴约束并扣除左右 padding，月份分组页不需要改成嵌套滚动；收藏夹详情和喜欢使用 Sliver 版本，浏览历史使用普通版本，三页均把真实 `tileExtent` 传给封面。
- 原月份归档、下拉刷新、提前分页、收藏失效提示、取消喜欢、浏览历史清理、批量下载与详情跳转逻辑保持不变；手机保持三列，宽屏只增加合理列数，不无限拉伸图片。
- 320px 宽并含 12px 两侧 padding 时，普通网格和 Sliver 网格均生成三个 94.67px 正方形且回调尺寸一致；公共几何、八个页面入口源码守卫与合集 Panel 共 6 项通过，目标文件无新增 error / warning，Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并检查收藏夹、喜欢和历史三页的封面裁切、月份分组、取消操作与滚动位置；按用户要求只处理 Android，桌面端未执行。

### P2B2B28：收藏夹管理卡片的窄屏适配（2026-08-29）

- “我的收藏夹”仍使用透明旧 Row：80px 封面、标题 / ID / 篇数和编辑 / 删除按钮全部挤在同一横轴，320px 或大字体下正文列被操作按钮持续压缩；同时缺少当前组件体系的白色内容表面和柔和边界。
- 新增公共 `LoftifyFavoriteFolderCard`，使用统一 `LoftifyCard` 白色表面、14px 卡片圆角、低噪声边界和无扩散水波纹按压；封面继续 `BoxFit.cover`，标题允许两行，ID 与篇数进入元数据层级，长按复制、详情跳转、编辑、删除和默认收藏夹不可删除的业务语义保持不变。
- 小于 420px 或文字缩放超过 1.35 倍时，封面缩至 72px，编辑 / 删除移到摘要下一行且继续使用圆形 48px 点击目标；正常宽度保持单行卡片，所有文本和卡片高度由内容驱动，不再以固定 80px 行高裁切。
- 320×480、2 倍字体与超长英文标题 / ID 下卡片无溢出、管理按钮可点击且不会误触卡片跳转；默认收藏夹危险操作隐藏和真实页面接入守卫共 3 项通过，公共组件零 error / warning（页面仅保留 6 项既有 info），Android Debug APK 构建成功。
- 真机 USB 仍离线，本小项尚待覆盖安装并检查真实收藏夹封面、长按复制、编辑 / 删除 Panel 和列表分页；按用户要求只处理 Android，桌面端未执行。
